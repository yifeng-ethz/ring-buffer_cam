# Integration Reports

Runtime logs are grouped by verification domain:

- `REPORT/rc/`: run-control and upload-link integration logs
- `REPORT/sc/`: slow-control smoke and burst-vs-single logs
- `REPORT/dp/`: datapath end-to-end logs

The checked-in documentation lives outside these runtime folders. Treat the
contents under the domain subdirectories as disposable evidence artifacts.
