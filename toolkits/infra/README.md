# Toolkit Infrastructure

Shared infrastructure used by board bring-up toolkits and generated metadata lives here.

- `cmsis_svd/`: reusable CMSIS-SVD helper library and generic bridge/register-window manifests.
- `board_bring_up/lib/`: small Tcl helpers shared by CSR metadata emitters and System Console dashboards.
