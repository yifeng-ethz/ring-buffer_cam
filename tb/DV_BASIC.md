# DV_BASIC.md — ring_buffer_cam

**Companion to:** [DV_PLAN.md](DV_PLAN.md), [DV_HARNESS.md](DV_HARNESS.md)
**Canonical ID Range:** `B001-B129`
**Intent:** deterministic functional contracts and state-machine invariants before stress and cross composition.

| case_id | method | implementation | legacy alias | scenario | primary checks |
|---|---|---|---|---|---|
| B001 | D | live UVM | `test_cfg_reset_defaults` | Reset-time CSR identity and control defaults | UID/META/CTRL/EXPECTED_LATENCY defaults and all counters are at expected zeroed state |
| B002 | D | live UVM | `test_cfg_rw_semantics` | Frontdoor CSR write/read semantics on writable and RO words | RW bits update, RO bits stable, soft-reset self-clear semantics |
| B003 | D | live UVM | `test_cfg_activity_counters` | Counter activity after baseline traffic | `INERR`, `PUSH`, `POP`, `OVERWRITE`, `CACHE_MISS` and `FILL_LEVEL` remain consistent |
| B004 | D | live UVM | `test_single_push_pop` | One accepted hit drains correctly through full protocol path | one hit in, one hit out, no residual references |
| B005 | D | live UVM | `test_same_key_burst_128` | same-key burst drains with correct subheader count | full 128 burst drains and no silent drop |
| B006 | D | live UVM | `test_same_key_burst_256` | deeper same-key burst exercises pipeline depth and counters | full 256 burst drains and expected latency remains bounded |
| B007 | D | live UVM | `test_sequential_keys` | multiple search keys drain in order across successive subheaders | every promoted key drains and scoreboard returns zero occupancy |
| B008 | D | live UVM | none | explicit run-control startup spine as a standalone case | `RUN_PREPARE -> RUN_SYNC -> RUNNING`, no stale state |

# Canonical Planned Cases (B009-B129)

| case_id | method | implementation | legacy alias | scenario | primary checks |
|---|---|---|---|---|---|
| B009 | D | live UVM | none | CSR[0] UID readback returns literal IP_UID generic (1380074317) on every read regardless of prior run-control activity | UID CSR word 0 equals IP_UID generic across reset, RUNNING, TERMINATING reads; catches accidental writability or mux swap with META |
| B010 | D | live UVM | none | CSR[1] META with `meta_sel=00` (VERSION) returns pack_version_func(26,2,0,421) bit layout | META word 1 reads `{major[31:24], minor[23:16], patch[15:12], build[11:0]}`; catches bit-field packing regression |
| B011 | D | live UVM | none | CSR[1] META with `meta_sel=01` (DATE) returns VERSION_DATE generic (20260421) | META word 1 reads the 32-bit date integer; catches a stale META mux leftover after meta_sel write |
| B012 | D | live UVM | none | CSR[1] META with `meta_sel=10` (GIT) returns VERSION_GIT generic | META reads git hash constant; catches mis-decoded 2-bit selector |
| B013 | D | live UVM | none | CSR[1] META with `meta_sel=11` (INSTANCE) returns INSTANCE_ID generic | META reads instance id constant; catches default-branch fallthrough when other selectors are live |
| B014 | D | live UVM | none | CSR[2] CTRL bit[0] `go` reset default = 1 verified before any write | CSR[2] read after reset yields bit[0]=1; catches a silent default flip that would block all ingress writes at startup |
| B015 | D | live UVM | none | CSR[2] CTRL bit[1] `soft_reset` self-clears within one idle cycle after host write-1 | write CTRL=0x2, immediate readback may be 1, but after one idle cycle CTRL bit[1]=0; catches a missing `csr.soft_reset <= '0'` in the idle-else branch |
| B016 | D | live UVM | none | CSR[2] CTRL bit[4] `filter_inerr` reset default = 1 | CSR[2] read after reset yields bit[4]=1; catches a regression that would let tserr hits into the deassembly fifo by default |
| B017 | D | live UVM | none | CSR[2] CTRL bits[3:2] and bits[31:5] unused inertness check | write CTRL=0xFFFF_FFFF and read back: only bits [0], [1] (readback before self-clear), [4] are set; catches accidental mapping of spare bits |
| B018 | D | live UVM | none | CSR[3] EXPECTED_LATENCY reset default = 2000 (`to_unsigned(2000,16)`) | CSR[3] read after reset = 0x000007D0; catches a regression of the reset literal |
| B019 | D | live UVM | none | CSR[3] EXPECTED_LATENCY full 16-bit writable range (no RTL clamp) using 0x0000 and 0xFFFF | write/read both extremes; catches a silent added clamp or width truncation |
| B020 | D | live UVM | none | CSR[3] EXPECTED_LATENCY mid-range write 0x4000 while RUNNING and verify `read_time_ptr` shifts by the same delta | POP_COUNT rate change after write; catches expected_latency_48b zero-padding bug |
| B021 | D | live UVM | none | CSR[4] FILL_LEVEL tracks `push_cnt - pop_cnt - overwrite_cnt` after N pushes with no pops | FILL_LEVEL reads = N; catches a stale latch or wrong subtraction sign |
| B022 | D | live UVM | none | CSR[4] FILL_LEVEL is write-ignored (WI) per proc_avmm_csr case branch | write CTRL_LEVEL=0xDEAD_BEEF then read: value unchanged from previous tick; catches a stray assignment on the write branch |
| B023 | D | live UVM | none | CSR[5] INERR_COUNT increments only when `filter_inerr=1` AND `asi_hit_type1_error(0)=1` is seen | drive 1 errored beat with filter on -> count=1; same beat with filter off -> count unchanged; catches missing AND-gate |
| B024 | D | live UVM | none | CSR[5] INERR_COUNT is cleared when run-control enters RUN_PREPARE (flush, `decision_reg=3`) | set count>0, issue PREP, verify count=0 post-PREP; catches missing INERR clear in proc_avmm_csr flush branch |
| B025 | D | live UVM | none | CSR[6] PUSH_COUNT increments only when arbiter grants `decision_reg=0` (push_write), never on accepted-but-not-granted cycles | N push beats all granted -> count=N; catches double-count from comb decision |
| B026 | D | live UVM | none | CSR[7] POP_COUNT increments only on `decision_reg=2` (pop_erase), not on cache-miss drains | inject one real hit + one cache-miss -> POP_COUNT=1 not 2; catches coupling with cache_miss_pulse |
| B027 | D | live UVM | none | CSR[8] OVERWRITE_COUNT increments on `decision_reg=1` (push_erase) triggered by `addr_occupied=1` on the write pointer | force wraparound on pointer into an occupied slot -> count=1 per overwrite; catches wrong decision-reg decode |
| B028 | D | live UVM | none | CSR[8] OVERWRITE_COUNT write-to-offset ignored (commented-out WO reset branch) | write 0xFFFF_FFFF to OFFSET 8, verify count does not reset; confirms documented behavior that WO-reset is not wired |
| B029 | D | live UVM | none | CSR[9] CACHE_MISS_COUNT increments on `pop_cache_miss_pulse` when DRAIN reads a hit with side_ram.occupancy=0 | patch one written side-RAM slot stale and verify `PUSH=1`, `POP=1`, `CACHE_MISS=1`, `FILL_LEVEL=0`, and one cache-miss-tagged output beat; catches missing pulse in proc_avst_output_assembly |
| B030 | D | live UVM | none | Counters 6/7/8/9 all clear synchronously when FLUSHING is entered (proc_debug_counter decision=3 branch) | accumulate non-zero `PUSH`/`POP`/`OVERWRITE`/`CACHE_MISS`, wait for the final overwrite retire, then enter FLUSHING and read all four counters as 0; catches asymmetric clear omission |
| B031 | D | live UVM | none | CSR[0] UID and CSR[1] META correlated readback in the same transaction pair: UID stable across a concurrent META selector write | write META sel=01, read UID word 0 same cycle window; UID must still be IP_UID; catches readdata mux contamination |
| B032 | D | live UVM | none | CSR waitrequest protocol: `avs_csr_waitrequest` drops to 0 the cycle after any read or write, returns to 1 in idle | observe waitrequest waveform across a 3-beat CSR sequence; catches a stuck waitrequest that would hang hub |
| B033 | D | live UVM | none | run-control: payload `000000001` (IDLE) at reset produces immediate `asi_ctrl_ready` ack | ready pulses 1 cycle while valid asserted; catches IDLE-branch ready omission |
| B034 | D | live UVM | none | run-control edge: IDLE -> RUN_PREPARE on payload `000000010` asserts both `pop_cmd_fifo_sclr` and `deassembly_fifo_sclr` | observe sclr pulses during PREP entry; catches missing fifo reset assertion |
| B035 | D | live UVM | none | run-control edge: PREP -> PREP-ready requires all of `pop_cmd_fifo_empty`, `deassembly_fifo_empty`, `debug_msg2.cam_clean`, `run_mgmt_flushed` | withhold one of the four, verify ready stays 0; catches a premature ready that bypasses the flush handshake |
| B036 | D | live UVM | none | run-control edge: PREP flush path asserts `run_mgmt_flush_memory_start` for one pop_engine FLUSHING round, then latches `run_mgmt_flushed='1'` | observe flushing-start rises once per PREP entry; catches a re-trigger loop |
| B037 | D | live UVM | none | run-control edge: RUN_PREPARE -> SYNC on payload `000000100` asserts `gts_counter_rst='1'` | observe gts_8n held at 0 during SYNC; catches missing gts clear |
| B038 | D | live UVM | none | run-control edge: SYNC -> RUNNING on payload `000001000` deasserts `gts_counter_rst` and clears `run_mgmt_flushed` | gts_8n begins counting; catches a stuck-cleared gts |
| B039 | D | live UVM | none | run-control edge: RUNNING -> TERMINATING on payload `000010000` captures `gts_end_of_run <= gts_8n` on entry cycle | read gts_end_of_run snapshot matches gts at entry; catches latch-on-every-cycle bug |
| B040 | D | live UVM | none | run-control edge: TERMINATING ready deferred until `terminating_drain_done=1` (endofrun_seen & fifos empty & pop IDLE & push=pop+ow) | withhold endofrun_seen, observe ready stays 0; catches premature TERMINATE ack |
| B041 | D | live UVM | none | run-control edge: TERMINATING -> IDLE on payload `000000001` clears `endofrun_seen` via the reset branch | send endofrun then IDLE cmd and verify flag resets; catches missing clear-on-IDLE |
| B042 | D | live UVM | none | run-control edge: IDLE -> RESET on payload `010000000` drops `asi_ctrl_ready` immediately (per RESET branch) | observe ready=1 on the RESET entry cycle; catches the commented stuck-host-recovery behavior |
| B043 | D | live UVM | none | run-control illegal payload (e.g. `000000011`) maps to `ERROR` state | observe run_state_cmd transitions to ERROR and ctrl_ready stays 0; catches a missing others-branch fallthrough |
| B044 | D | live UVM | none | run-control idempotency: duplicate `RUN_PREPARE` commands do not retrigger `run_mgmt_flush_memory_start` once `run_mgmt_flushed=1` | flush_start stays 0 on the 2nd PREP; catches a double-flush regression |
| B045 | D | live UVM | none | run-control idempotency: duplicate `SYNC` commands keep `gts_counter_rst` asserted without glitches | gts_8n remains 0 across both commands; catches a reset toggle |
| B046 | D | live UVM | none | run-control idempotency: duplicate `RUNNING` commands do not reset run_mgmt_flushed twice (only one clear per transition) | count run_mgmt_flushed rising edges = 1; catches redundant clear writes |
| B047 | D | live UVM | none | run-control bypass: IDLE -> RUNNING opens ingress immediately even though PREP/SYNC were skipped | direct RUNNING accepts pushes, leaves `run_mgmt_flushed=0`, and a subsequent PREP flush clears the partial run state | catches accidental gating changes around the unsupported shortcut path |
| B048 | D | live UVM | none | run-control partial spine: PREP -> RUNNING (skipping SYNC), then late SYNC recovery | PREP->RUNNING accepts pushes with `gts_counter_rst=0`; a later `SYNC -> RUNNING` resets `gts_8n` and drains the preserved backlog cleanly | catches stale timestamp-base handling when SYNC is deferred |
| B049 | D | live UVM | none | run-control: `LINK_TEST` (`000100000`) and `SYNC_TEST` (`001000000`) fall into the `others=>` branch, leaving ctrl_ready=0 | observe no ack; catches accidental implementation drift that would ack these untested states |
| B050 | D | live UVM | none | run-control: `OUT_OF_DAQ` (`100000000`) falls into `others=>` branch, no ingress accepted | PUSH_COUNT stays 0 while this state is active; catches wrong routing of this command |
| B051 | D | live UVM | none | ingress gate: `csr.go=0` blocks `deassembly_fifo_wrreq` even in RUNNING | clear go bit, inject beats, verify PUSH_COUNT does not advance; catches a missing go-gate in proc_avst_input_deassembly_comb |
| B052 | D | live UVM | none | ingress interleaving: only beats with `ts[TCC8N_INTERLEAVING_HI:LO] = INTERLEAVING_INDEX` reach the fifo | mix two ts patterns, verify only matching-interleave count accepted; catches lane mismatch |
| B053 | D | live UVM | none | ingress empty-marker: `asi_hit_type1_empty=1` with matching `channel[1:0]` bypasses the data-based lane match and sets `endofrun_seen` on EOP | verify endofrun_seen registers on channel-index empty-EOP; catches wrong channel decode on empty marker |
| B054 | D | live UVM | none | push pipeline depth 1: single hit, verify one-cycle push_write, no ERASE state entered (`addr_occupied=0` at write_pointer) | decision_reg sequence {0,4} only, OVERWRITE_COUNT stays 0; catches spurious erase |
| B055 | D | live UVM | none | push pipeline depth 2: one hit followed by pointer wrap onto an occupied slot, state enters ERASE for one cycle | decision_reg sequence {0,1,0,...}, OVERWRITE_COUNT=1; catches missed overwrite detection |
| B056 | D | live UVM | none | pop search latency baseline: `SEARCH` state counts `pop_search_wait_cnt` 0..5 before snapshotting match vector | measure IDLE->SEARCH->LOAD cycle count = 6; catches altered wait guard that would sample stale cam_match_addr_oh |
| B057 | D | live UVM | none | pop LOAD state walks `pop_load_idx` 0..N_PARTITIONS-1, asserting `pop_partition_load(i)` exactly once per live partition of the active build | count load pulses = `N_PARTITIONS` of the selected RTL variant (`2` on `default_p2_pipe4`); catches skipped partition |
| B058 | D | live UVM | none | pop COUNT: MATCH_COUNT_CHUNKS_CONST chunks accumulated per partition using `count_ones_16` | seed CAM with 17 hits in one partition and verify total_hits=17; catches off-by-one in chunk iteration |
| B059 | D | live UVM | none | pop COUNT zero-hit fast path: if `pop_count_total_acc=0` engine transitions COUNT -> RESET and asserts `pop_cmd_fifo_rdack` | observe one-cycle rdack and no DRAIN entry; catches a spurious DRAIN on empty search |
| B060 | D | live UVM | none | subheader framing for empty drain: SOP+EOP same beat, hit_count field = 0x00, K237 lane = 0xF7 | capture first egress beat after cache miss; catches SOP-only framing regression |
| B061 | D | live UVM | none | subheader framing for 1-hit drain: SOP on subheader beat, EOP on hit beat, hit_count field = 0x01 | two egress beats with correct SOP/EOP; catches EOP-on-subheader regression when hits>0 |
| B062 | D | live UVM | none | subheader `data[31:24]` carries `pop_current_sk[7:0]` = ts[11:4] of the drained epoch | push hit at ts=0x2A0, verify subheader ts byte = 0x2A; catches wrong slice |
| B063 | D | live UVM | none | subheader `data[15:8]` carries `pop_total_hits[7:0]` for non-zero drain | push 5 hits same key, subheader hit_count byte = 0x05; catches truncation |
| B064 | D | live UVM | none | subheader `data[7:0]` = K237 (0xF7) constant | verify on every subheader beat; catches identifier regression |
| B065 | D | live UVM | none | subheader `data[35:32]` (byte_is_k) = "0001" on subheader, "0000" on hits | capture k-bits across subheader+hit sequence; catches mixed k-flag bug |
| B066 | D | live UVM | none | hit egress bit layout: `data[31:28]=ts[3:0]`, `[27:22]={00, asic[3:0]}`, `[21:17]=channel[4:0]`, `[16:9]=tcc1n6+tfine`, `[8:0]=et1n6` | drive a known hit payload, verify each field; catches slice regression from revision 2.x |
| B067 | D | live UVM | none | cache-miss mid-drain: drive cmd whose key matches CAM entry but where one live side-RAM slot is patched to occupancy=0 before drain, so `pop_cache_miss_pulse` fires mid-epoch | `CACHE_MISS_COUNT=1`, subheader hit_count remains 2, both hit beats still forward, and the final beat carries `cache_miss` + `EOP` | catches missing occupancy check in proc_avst_output_assembly |
| B068 | D | live UVM | none | tsglitcherr isolation: good-hit / glitch-hit / good-hit window under one search key | exactly one of the three drained hits asserts `aso_hit_type2_error[0]`, and the adjacent good hits remain clean | catches error leakage beyond the offending beat |
| B069 | D | live UVM | none | pop descriptor cadence: in RUNNING, command fifo gets one write per `read_time_ptr[3:0]=0000` AND lane match | count cmd writes over 256 cycles = 256/(16*INTERLEAVING_FACTOR); catches missing modulo guard |
| B070 | D | live UVM | none | pop descriptor boundary: first pop fires at `read_time_ptr=0` exactly (ts[11:4]=0 bin not skipped) per revision 2.9 | observe first subheader ts byte = 0x00; catches return of the skip-first bug |
| B071 | D | live UVM | none | terminating_drain_done combinational condition: all blocking conjuncts must clear together before ready returns in TERMINATING | observe each blocker false while `terminating_drain_done=0`, then only assert ready once all clear; catches loose OR of conditions |
| B072 | D | live UVM | none | deassembly_fifo backpressure: `asi_hit_type1_ready` drops only when `deassembly_fifo_full`, then recovers once full clears | fill the deassembly FIFO under overwrite pressure and prove `ready` mirrors `fifo_full` only; catches spurious ready toggles |
| B073 | D | live UVM | none | same-key occupancy = 1: push 1, pop 1, scoreboard FILL_LEVEL trajectory {0,1,0} observed over CSR polls | catches FILL_LEVEL update lag beyond 1 cycle |
| B074 | D | live UVM | none | same-key occupancy = 2: push 2 same key, pop at next latency boundary yields one subheader with hit_count=2 | subheader hit_count byte = 0x02; catches partial drain |
| B075 | D | live UVM | none | same-key occupancy = 3 with the write pointer pre-advanced to the live partition boundary: verify pending and issue walk across two partitions in DRAIN | two partitions become pending and both issue under one isolated drain; catches deadlock where one partition blocks |
| B076 | D | live UVM | none | same-key occupancy = LEAF_WIDTH (16): fills exactly one encoder leaf in one partition | subheader hit_count byte = 0x10, all 16 drained; catches leaf-count off-by-one |
| B077 | D | live UVM | none | same-key occupancy = LEAF_WIDTH+1 (17): crosses leaf boundary within partition, exercises `has_more` from first leaf | count all 17 drains, latency per hit stable; catches has_more mis-propagation |
| B078 | D | live UVM | none | same-key occupancy = MATCH_PARTITION_SIZE computed from the active `RING_BUFFER_N_ENTRY/N_PARTITIONS`: exactly fills one partition | partition encoder drains one full partition and the next partition never becomes pending; catches partition leakage |
| B079 | D | live UVM | none | same-key occupancy = MATCH_PARTITION_SIZE+1 from the active build: asserts the second partition pending bit on the same search key | 2 partitions with pending=1 are observed in `pop_partition_pending`; catches boundary off-by-one |
| B080 | D | live UVM | none | same-key occupancy = `RING_BUFFER_N_ENTRY/2`; for the p2 build the pointer is pre-advanced so the half-ring spans both live partitions evenly | both live partitions issue during DRAIN and round-robin advances to the second partition; catches partition-ordering bias |
| B081 | D | live UVM | none | same-key occupancy = RING_BUFFER_N_ENTRY-1 (511): drains 511 hits with subheader hit_count[7:0] wrapping at 255 boundary | subheader hit_count shows truncated lower byte of 511 (0xFF); confirms documented truncation |
| B082 | D | live UVM | none | same-key wraparound: write_pointer wraps the full ring while same key is hot, triggers repeated overwrites of prior same-key entries | OVERWRITE_COUNT increments per wrap, final drain count matches alive entries; catches double-counted overwrite |
| B083 | D | live UVM | none | same-key with EXPECTED_LATENCY=0: pop fires immediately at each 16-cycle boundary | pop cadence independent of gts_8n alignment; catches signed-compare regression |
| B084 | D | live UVM | none | same-key with EXPECTED_LATENCY mid-run change: write new latency while one hit is pending, verify the already-buffered hit drains on the rewritten latency contract | no early subheader before the rewrite, then immediate release after latency=0; catches a latched-latency bug |
| B085 | D | live UVM | none | same-key with filter_inerr toggled mid-burst: first half filtered, second half accepted | INERR_COUNT = N_error_first_half, PUSH_COUNT = N_good total; catches state persistence across CTRL writes |
| B086 | D | live UVM | none | pop DRAIN round-robin: one partition with 2 hits, next partition idle, verify pop_rr_idx increments past the idle partition without stall | no extra-cycle stall on empty partition; catches empty-partition hang |
| B087 | D | live UVM | none | pop DRAIN advance handshake: after erase_grant, `pop_partition_advance(idx)` pulses exactly one cycle while has_more=1 | measure advance pulse width on a known partition; catches stuck-high advance |
| B088 | D | live UVM | none | pop DRAIN last-hit: `pop_last_hit_pending=1` on the cycle that `pop_hits_count=1`, drives EOP on the following emitted hit beat | observe `pop_last_hit_pending` before the final hit and EOP on that final hit; catches a missed EOP pulse |
| B089 | D | live UVM | none | pop RESET state: clears pop_partition_snapshot, pending, total_hits, count_done, issue_valid in one cycle | pop_engine_state observed {RESET,IDLE} with the pop context zeroed in RESET; catches any field leaking into next IDLE |
| B090 | D | live UVM | none | push-pop non-exclusivity: push_write allowed while pop engine is in SEARCH/LOAD/COUNT (rev 2.4 relaxation) | push_write_grant observed during SEARCH with pop_engine_state not IDLE; catches the old exclusive lock |
| B091 | D | live UVM | none | push-pop exclusivity: push_write blocked exactly when pop_erase holds grant in DRAIN | decision=2 wins over decision=0 in DRAIN; catches priority regression in proc_memory_arbiter_comb |
| B092 | D | live UVM | none | arbiter priority: simultaneous `push_erase` + `pop_erase` grants `push_erase` and suppresses `pop_erase` in DRAIN | `push_erase_grant=1`, `pop_erase_grant=0`, and overwrite accounting advances under overlap; catches pop-erase overriding the data-dependency path |
| B093 | D | live UVM | none | arbiter priority: flush asserted during active DRAIN abandons pop_erase immediately | decision=3 wins, pop_engine_state transitions via FLUSHING; catches stuck in DRAIN under flush |
| B094 | D | planned | none | partitioned encoder PIPE_STAGES=1 baseline: o_result_valid reappears one cycle after i_load for a single-hit snapshot | measure i_load -> o_result_valid latency = 1; catches regression in ACTIVE_PIPE_STAGES min/max clamp |
| B095 | D | planned | none | partitioned encoder PIPE_STAGES=2 baseline: single-hit latency = 2 cycles | same as B094 with PIPE_STAGES=2; catches drop of the extra_valid shift register |
| B096 | D | planned | none | partitioned encoder PIPE_STAGES=3 baseline: single-hit latency = 3 cycles | same as B094/B095 with PIPE_STAGES=3; catches mis-mapped stage3 flag/lsb assignments |
| B097 | D | live UVM | none | partitioned encoder PIPE_STAGES=4 baseline (default): a non-zero `eval_match_stage0` snapshot is surfaced and the flagged `result_valid` returns 3 cycles later on the active partition only | observe the stage0 intermediate and the active-partition-only result-valid pulse; catches lost stage0 register or partition leakage |
| B098 | D | live UVM | none | partitioned encoder exactly-one-partition-active: only one active partition carries matches while the other active partition stays empty | only the matching partition's flag/result-valid path pulses; catches spurious flag assertion from an empty partition |
| B099 | D | live UVM | none | partitioned encoder exactly-two-partitions-equal-load: each has 4 matches, drain alternates via round-robin | 8 total drains interleave 1-1-1-1-1-1-1-1; catches imbalanced RR |
| B100 | D | live UVM | none | partitioned encoder idle-on-one-while-another-drains: partition 0 carries the full drain epoch while the other active partition stays empty | `pop_rr_idx` stays on the active partition until exhaustion; catches unnecessary idle visits |
| B101 | D | live UVM | none | partitioned encoder full-round-robin rotation across all active partitions in the current build | verify `pop_issue_partition_idx` walks `0..N_PARTITIONS-1` without skipping an active partition |
| B102 | D | live UVM | none | partitioned encoder clear_onehot_bit: after `i_advance`, the reported LSB address is removed from the active match vector | second reported address must differ from the first; catches stale reporting of the same leaf bit |
| B103 | D | live UVM | none | partitioned encoder `has_more` falls from `1` to `0` on the last remaining match of a partition | observe the final transition on the surfaced `has_more` flag; catches wrong end-of-leaf bookkeeping |
| B104 | D | planned | none | partitioned encoder i_load overrides pending advance: new i_load while has_more pending resets the pipeline to the new vector | next reported LSB matches new vector's LSB, not old has_more address; catches missing load priority in start_eval |
| B105 | D | live UVM | none | FLUSHING 2D walk: `flush_cam_wraddr` increments only when `flush_cam_wrdata` rolls over `POP_FLUSH_CAM_DATA_MAX`, then wraps to `0` on the terminal line | catches premature address increment and wrong completion rollover semantics |
| B106 | D | live UVM | none | FLUSHING RAM walk: `flush_ram_wraddr` increments once per grant, reaches the terminal line, then wraps to `0` when `pop_flush_ram_done` asserts | catches skipped address steps and wrong terminal rollover semantics |
| B107 | D | live UVM | none | FLUSHING_RST state: preempt an active drain with RUN_PREPARE and verify pop_partition context is cleared before the engine returns to IDLE | observe pop_engine_state sequence {DRAIN, FLUSHING, FLUSHING_RST, IDLE}; catches missing flag clears |
| B108 | D | live UVM | none | FLUSHING completion handshake: run_mgmt_flush_memory_done asserts one cycle after both ram and cam done | asi_ctrl_ready for PREP latches after this pulse; catches missing done pulse |
| B109 | D | live UVM | none | end-to-end flow: PREP -> SYNC -> RUNNING, inject 1 hit, drain, TERMINATING, verify PUSH_COUNT=POP_COUNT=1 and OVERWRITE=0 | counters consistent and endofrun clean; catches state leakage |
| B110 | D | live UVM | none | end-to-end flow in one RUNNING epoch: inject 10 distinct keys with one hit each and verify 10 ordered data-bearing subheaders with hit_count=1 | each subheader carries the expected search key and K237 marker; catches mixed-epoch drain |
| B111 | D | live UVM | none | end-to-end flow with mid-run EXPECTED_LATENCY ramp: drain one hit at latency=2000, rewrite to 1000 without restart, then verify the next hit drains sooner | observed drain window shortens after the rewrite; catches a latched-latency bug |
| B112 | D | live UVM | none | end-to-end flow with filter_inerr mid-run toggle: first half filtered and counted in INERR, second half accepted and drained after filter disable | INERR_COUNT reflects only the filtered-on half while PUSH/POP reflect the accepted half; catches persistent filter state |
| B113 | D | live UVM | none | end-to-end flow with PREP -> SYNC -> RUNNING then TERMINATING without injecting any hit: `terminating_drain_done` latches promptly on the empty end-of-run marker | TERMINATING ack cycle count bounded; catches long-tail wait on empty run |
| B114 | D | live UVM | none | end-to-end flow with endofrun seen on the empty-marker path while hits are still pending: TERMINATING drains those hits before ack | pending hits drain first, ack follows; catches early-ack race |
| B115 | D | live UVM | none | end-to-end flow restart cycle: PREP -> SYNC -> RUN -> TERMINATING -> IDLE -> PREP (second pass), all counters reset through FLUSHING between passes | counters observed = 0 at start of second RUN; catches residual counter state |
| B116 | D | live UVM | none | end-to-end flow: FILL_LEVEL trajectory follows the integer model across a 16-hit fill window and the corresponding full drain | FILL_LEVEL CSR polls match the predicted fill ramp and return to zero after terminate; catches latched-stale subtraction |
| B117 | D | live UVM | none | end-to-end flow subheader phase boundary: run for exactly one latency window, verify exactly one subheader generated and it carries the correct ts byte | count = 1 subheader, ts byte = expected sk; catches duplicate subheader |
| B118 | D | live UVM | none | end-to-end flow with empty subheader injection: one cmd has zero matches, egress emits SOP+EOP subheader with hit_count=0 and no hits | scoreboard records the zero-hit subheader, cache miss counter unchanged (only real stale-occupancy miss counts); catches spurious CACHE_MISS increment |
| B119 | D | live UVM | none | end-to-end flow where push rate > pop rate until FILL_LEVEL stabilizes at a non-zero plateau (no overwrite) | plateau observed, OVERWRITE_COUNT stays 0; catches phantom overwrite |
| B120 | D | live UVM | none | end-to-end flow where pop rate > push rate leaves FILL_LEVEL at 0 and no cache miss (all real hits are drained exactly once) | CACHE_MISS_COUNT=0, POP_COUNT=PUSH_COUNT; catches spurious cache-miss pulse on real drains |
| B121 | D | live UVM | none | end-to-end flow with channel-index empty-marker endofrun only on the matching INTERLEAVING_INDEX lane | only the correct lane's endofrun_seen latches; catches cross-lane leakage in run_mgmt |
| B122 | D | live UVM | none | end-to-end flow with subsequent PREP after TERMINATE ack: PREP re-asserts flush even if cam is already clean (run_mgmt_flushed starts low) | flush pulse observed again; catches optimization that would skip second flush |
| B123 | D | live UVM | none | end-to-end flow covering all four meta_sel values in one READ walk (00/01/10/11) | META VERSION / DATE / GIT / INSTANCE pages are returned in selector order without mux stickiness |
| B124 | D | live UVM | none | end-to-end flow where CTRL soft_reset is pulsed during active DRAIN; soft_reset aborts the drain, clears live state, and self-clears bit1 while preserving the programmed CTRL bits | observe return to `IDLE`, pop-engine idle, cleared counters/FIFOs/fill level, and CTRL readback settling to `0x11`; catches stale descriptor or drain activity surviving soft_reset |
| B125 | D | live UVM | none | end-to-end flow: aso_filllevel stream asserts valid every cycle once i_rst=0, data mirrors CSR fill_level[15:0] | filllevel valid = 1 and data matches FILL_LEVEL CSR lower half; catches a dropped filllevel valid |
| B126 | D | live UVM | none | end-to-end flow: aso_hit_type2_channel field on both subheader and hit egress beats equals INTERLEAVING_INDEX generic | channel byte matches generic on every beat; catches regression that would forward the asic index instead |
| B127 | D | live UVM | none | end-to-end flow: counter bounded-delay contract - PUSH_COUNT visible at CSR within 2 CSR-read cycles of the granted push_write | measure delay from decision_reg=0 to CSR increment visibility; catches a multi-cycle pipeline bug in debug_msg2 |
| B128 | D | live UVM | none | end-to-end flow: pop_cmd_fifo depth=16 soak under RUNNING - descriptor rate matches consumption so fifo never exceeds 11 (per RTL comment for 960kHz case) | pop_cmd_fifo_usedw peak observed <= 11 over 100k cycles at 960kHz-equivalent stimulus; catches cadence regression |
| B129 | D | live UVM | none | end-to-end flow: decision_reg=4 (idle) occurs only when no engine is requesting, never between two back-to-back push_write grants | decision_reg trace between consecutive accepts never shows 4; catches bubble insertion in arbiter |
