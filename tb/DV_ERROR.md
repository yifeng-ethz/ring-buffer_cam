# DV_ERROR.md — ring_buffer_cam

**Companion to:** [DV_PLAN.md](DV_PLAN.md), [DV_HARNESS.md](DV_HARNESS.md)
**Canonical ID Range:** `X001-X129`
**Intent:** fault, legality, terminate/flush/restart, and recovery scenarios.

| case_id | method | implementation | legacy alias | scenario | primary checks |
|---|---|---|---|---|---|
| X001 | D | live UVM | old `D018-D019` intent | filter_inerr on/off behavior under injected bad-hit patterns | accepted vs filtered error hits and `INERR_COUNT` behavior |
| X002 | D | live UVM | old `D020` intent | timestamp glitch on drained hit | output error bit flags and ts mismatch handling |
| X003 | D | live UVM | `test_overwrite_stress` | GOOD then TERMINATE then residual drain | old hits drain, fresh accepts stop when required |
| X004 | D | live UVM | old `D022` intent | flush and immediate restart | no stale data leaks across restart boundary |
| X005 | D | planned | old `D032` intent | soft-reset in active traffic context | state returns to idle contract and counters reset coherently |
| X006 | D | live UVM | run-control decode intent | illegal or premature control word injection | illegal commands are contained and state remains legal |

# Canonical Planned Cases (X007-X129)

| case_id | method | implementation | legacy alias | scenario | primary checks |
|---|---|---|---|---|---|
| X007 | D | live UVM | none | single `asi_hit_type1_error(0)=1` pulse during RUNNING with `filter_inerr=1`, DUT idle (no in-flight push/drain) — expect filter in `proc_avst_input_deassembly_comb` to suppress `deassembly_fifo_wrreq` and `proc_csr_rw` to bump `inerr_cnt` | `INERR_COUNT` = 1 within 2 clk; `PUSH_COUNT` unchanged; `FILL_LEVEL` unchanged; scoreboard outcome: no accepted ingress hit recorded |
| X008 | D | live UVM | none | same single bad-hit injection but with `filter_inerr=0` (CTRL bit4=0) — the error path must pass through: deassembly_fifo_wrreq asserts, push occurs, and `inerr_cnt` must NOT increment | `INERR_COUNT` unchanged; `PUSH_COUNT` +1 within 4 clk; scoreboard: ingress hit recorded with error-flag metadata; later drain must carry `aso_hit_type2_error(0)` = 1 only if ts[12] mismatch |
| X009 | D | live UVM | none | bad-hit concurrent with a good-hit one cycle later, same search key, `filter_inerr=1` — bad hit drops at deassembly; good hit pushes | `INERR_COUNT` = 1; `PUSH_COUNT` = 1; scoreboard: only good hit in reference queue; drain of the sk emits exactly 1 hit beat after subheader |
| X010 | D | live UVM | none | back-to-back bad hits (2 consecutive cycles), `filter_inerr=1`, during RUNNING — confirm `inerr_cnt` accumulates without double-pumping or skipping | `INERR_COUNT` = 2 within 3 clk; `PUSH_COUNT` unchanged; scoreboard: 0 ingress hits recorded |
| X011 | D | live UVM | none | back-to-back bad hits with `filter_inerr=0` — both must reach the CAM via deassembly_fifo | `PUSH_COUNT` +2; `INERR_COUNT` unchanged; scoreboard: both hits recorded; `FILL_LEVEL` = 2 |
| X012 | D | live UVM | none | alternating good/bad pattern (G,B,G,B,G,B) 6 cycles, `filter_inerr=1` — interleave must not corrupt fifo write strobes | `INERR_COUNT` = 3 within 4 clk; `PUSH_COUNT` = 3; scoreboard: exactly 3 GOOD references; every drained hit matches a GOOD sk |
| X013 | D | live UVM | none | bad hit with `asi_hit_type1_channel` NOT matching `INTERLEAVING_INDEX` — the lane_match_v guard should drop the hit before the error-filter ever sees it | `INERR_COUNT` unchanged (lane-miss short-circuits the filter); `PUSH_COUNT` unchanged; scoreboard: hit not recorded |
| X014 | D | live UVM | none | bad hit with `asi_hit_type1_empty=1` (end-of-run marker beat) and error=1 — confirm endofrun_seen decode path is not spoofed into bumping inerr_cnt on the empty beat | `INERR_COUNT` unchanged on empty beat; `endofrun_seen` assertion still latched if channel matches; `PUSH_COUNT` unchanged |
| X015 | D | live UVM | none | bad hit exactly on the cycle `push_write_grant=1` for a prior good hit — arbiter must not corrupt the previous push while the error is filtered | `PUSH_COUNT` = 1 for the good hit within 4 clk; `INERR_COUNT` = 1; `OVERWRITE_COUNT` = 0 |
| X016 | D | planned | none | bad hit while `deassembly_fifo_full=1` (ingress backpressure) — filter must still bump inerr_cnt even though wrreq is naturally masked | `INERR_COUNT` = 1 within 2 clk; `asi_hit_type1_ready` observed low in same cycle; scoreboard: no recorded accept |
| X017 | D | live UVM | none | bad hit during the single cycle when `decision_reg=3` (flushing) — flushing path clears inerr_cnt in `proc_csr_rw`; the injected event must be swallowed | `INERR_COUNT` = 0 post-flush (counter reset by flushing branch); `PUSH_COUNT` unchanged; scoreboard: hit not accepted |
| X018 | D | planned | none | bad hit on the boundary cycle `run_state_cmd` transitions IDLE→RUN_PREPARE — deassembly gate is closed (csr.go off or run_state_cmd!=RUNNING); error must not bump inerr_cnt unless filter_inerr decision is still active in proc_csr_rw | `INERR_COUNT` tracks filter_inerr raw error as specified in `proc_csr_rw` (independent of `run_state_cmd`); `PUSH_COUNT` unchanged; asi_ctrl_ready asserted in RUN_PREPARE only after flush completes |
| X019 | D | planned | none | bad hit on the cycle `run_state_cmd` enters RUNNING — confirm the first cycle after RUNNING accepts neither the stale bad hit nor a stray push | `INERR_COUNT` tracks the raw bad-hit error pulse; `PUSH_COUNT` = 0 until the first GOOD hit; scoreboard: no phantom accept |
| X020 | D | planned | none | bad hit during an in-flight pop DRAIN (decision_reg=2) — the bad ingress must not disturb the drain output; aso_hit_type2_error for the in-flight hit must reflect ONLY the ts[12] check | `INERR_COUNT` += 1; `POP_COUNT` matches expected drained count; drained beat carries the ts[12]-derived error bit, not the ingress error |
| X021 | D | planned | none | bad hit arriving the same cycle as a subheader emission (pop_cmd_fifo_rdack=1, subheader_gen_done=0) | aso_hit_type2 subheader byte pattern unchanged (K237 tail, pop_current_sk in bits 31:24); `INERR_COUNT` += 1; subheader count field equals total_hits for the sk |
| X022 | D | planned | none | bad hit race with a CSR read of `EXPECTED_LATENCY` (address 3) — read must not stall or corrupt inerr_cnt update | `EXPECTED_LATENCY` read data matches constant; `INERR_COUNT` += 1 within 2 clk; `avs_csr_waitrequest` deasserts as normal |
| X023 | D | planned | none | bad hit race with a CSR write to CTRL flipping filter_inerr from 1→0 on the SAME cycle the bad hit arrives | observe the pre-write value of `filter_inerr` governs the current cycle: if pre-write was 1, `INERR_COUNT` += 1 and hit dropped; scoreboard mirrors the same boundary rule |
| X024 | D | planned | none | bad hit race with a CSR write of INERR_COUNT (self-clearing semantics — reg has no write side-effect per `proc_csr_rw`, so counter keeps incrementing) | `INERR_COUNT` += 1 despite the concurrent write; no spurious clear; scoreboard: bad-hit accounting consistent |
| X025 | D | planned | none | 8 bad hits in a 10-cycle window with `filter_inerr=1`, all same search key | `INERR_COUNT` = 8 within 3 clk of last bad beat; `FILL_LEVEL` = 0; scoreboard: zero accepted references |
| X026 | D | planned | none | 8 bad hits in a 10-cycle window with `filter_inerr=0` followed by a pop drain of that sk — drained hits may carry aso_hit_type2_error due to ts[12] mismatch | `PUSH_COUNT` = 8, `OVERWRITE_COUNT` tracks if the same sk reuses an address; drained subheader count equals live residents; `aso_hit_type2_error` beat count matches ts[12] mismatches |
| X027 | D | planned | none | bad hit at the exact cycle `FILL_LEVEL` transitions from N to N+1 due to an independent good push — fill-level meter must not double-count the filtered error | `FILL_LEVEL` = N+1 (not N+2) within 3 clk; `PUSH_COUNT` += 1; `INERR_COUNT` += 1 |
| X028 | D | planned | none | bad hit at exact cycle `FILL_LEVEL=RING_BUFFER_N_ENTRY` (full ring) — filter drops the hit before it can trigger an overwrite | `OVERWRITE_COUNT` unchanged; `INERR_COUNT` += 1; `FILL_LEVEL` unchanged |
| X029 | D | planned | none | bad hit concurrent with an overwrite on the same address (another good hit collides at same CAM address) — two unrelated events on the same cycle; RTL must attribute them to separate counters | `OVERWRITE_COUNT` += 1 (from the good collision), `INERR_COUNT` += 1 (from the bad beat); scoreboard: one expected-overwrite, one filtered-error |
| X030 | D | planned | none | sustained bad-hit rate of 1-in-4 cycles over 256 cycles, `filter_inerr=1` — stress the filter edge and confirm no counter wrap / no spurious push | `INERR_COUNT` = 64; `PUSH_COUNT` = 0; `FILL_LEVEL` = 0; scoreboard: no accepted references |
| X031 | D | live UVM | none | TERMINATE command (`asi_ctrl_data=0x010`) with empty ring-buffer, no in-flight hits — `terminating_drain_done` must assert promptly; asi_ctrl_ready goes high; state enters TERMINATING | asi_ctrl_ready pulse within 3 clk of valid; `FILL_LEVEL` = 0; `POP_COUNT` unchanged; scoreboard: no pending references |
| X032 | D | planned | none | TERMINATE during same-key burst of 16 pushes on one sk — endofrun_seen latch allows the burst to drain; new hits must stop being accepted once endofrun_seen=1 | `POP_COUNT` ≤ 16 and equals number of unique residents; `PUSH_COUNT` monotonic then freezes; asi_ctrl_ready asserts after drain completes; scoreboard: terminate-aborted drain classification if any ref unfulfilled |
| X033 | D | planned | none | TERMINATE while drain engine owns the bus (pop_engine_state=DRAIN) — confirm in-flight subheader/hit emission completes before asi_ctrl_ready | last drained aso_hit_type2 beat has `endofpacket=1`; asi_ctrl_ready not asserted until after eop; `POP_COUNT` reflects all drained hits |
| X034 | D | planned | none | TERMINATE mid-subheader emission (subheader_gen_done=0 latched cycle) — subheader must complete its single-cycle emission; no truncated K237 beat | subheader beat contains correct K237 in bits [7:0] and `pop_current_sk` in [31:24]; asi_ctrl_ready follows after |
| X035 | D | planned | none | TERMINATE one cycle before a CSR write to CTRL (soft_reset=1) — ordering: TERMINATING state takes precedence over soft_reset since proc_csr_rw self-clears soft_reset | `run_state_cmd`=TERMINATING observable via run-state observer; soft_reset returns to 0 on the same cycle; `FILL_LEVEL`/counters follow TERMINATING drain, not the soft-reset branch |
| X036 | D | planned | none | TERMINATE one cycle after a CSR write to CTRL — CSR write must complete its waitrequest handshake before the run-state transition is visible | avs_csr_waitrequest deasserts within 1 clk of write; TERMINATING decode on next cycle; no lost CTRL fields |
| X037 | D | planned | none | TERMINATE while a cache-miss is being emitted (pop_cache_miss_pulse=1 same cycle) — the cache-miss beat must still bump `CACHE_MISS_COUNT` before terminate drain retires the engine | `CACHE_MISS_COUNT` += 1 within 3 clk; drained beat carries the cache-miss flag via `aso_hit_type2_error` if ts[12] mismatches; scoreboard: cache-miss classification on that beat |
| X038 | D | live UVM | none | two consecutive TERMINATE valids (redundant run-control command) — second TERMINATE must idempotently re-ack without double-draining | asi_ctrl_ready toggles twice; `POP_COUNT` increments exactly once per resident; no underflow on `FILL_LEVEL` |
| X039 | D | planned | none | TERMINATE then immediate RUN_PREPARE without an intervening IDLE — flush path triggers from RUN_PREPARE, endofrun_seen cleared on RUN_PREPARE entry | endofrun_seen=0 visible after RUN_PREPARE decode; run_mgmt_flushed asserted after pop engine completes FLUSHING_RST; asi_ctrl_ready held low until cam_clean and fifo empty |
| X040 | D | planned | none | TERMINATE then RUNNING directly (skip RUN_PREPARE) — per run-control contract RUNNING skips the flush; stale residents may remain until next RUN_PREPARE | `FILL_LEVEL` unchanged across the TERMINATE→RUNNING boundary; scoreboard classifies any residents as terminate-aborted drain carryover |
| X041 | D | planned | none | TERMINATE while `asi_hit_type1_valid=1` on the same cycle — deassembly gate closed because `run_state_cmd != RUNNING` branch disables wrreq; the arriving hit must not be accepted | `PUSH_COUNT` unchanged; `INERR_COUNT` unchanged (good hit, not bad); scoreboard: no accept recorded on that beat |
| X042 | D | planned | none | TERMINATE with `endofrun_seen=1` already latched from a prior empty beat — drain completes quickly since no new hits enter | `terminating_drain_done` asserts within latency of residents; asi_ctrl_ready within 2 clk of that; scoreboard drains cleanly |
| X043 | D | planned | none | TERMINATE with a full ring and 4 active partitions — drain engine walks all N_PARTITIONS via pop_count_partition_idx; confirm all residents exit | `POP_COUNT` = RING_BUFFER_N_ENTRY resident count; `FILL_LEVEL` = 0 at asi_ctrl_ready; scoreboard: no undrained refs |
| X044 | D | planned | none | TERMINATE immediately after a bad-hit burst (filter_inerr=1) — inerr_cnt survives TERMINATING (only flushing path clears it) | `INERR_COUNT` retained through TERMINATING; cleared only on entering RUN_PREPARE FLUSHING |
| X045 | D | planned | none | TERMINATE with a pop_cmd_fifo nonempty and deassembly_fifo nonempty — TERMINATING must not mask asi_ctrl_ready (only RUN_PREPARE gates ready on fifo emptiness) | asi_ctrl_ready path depends only on `terminating_drain_done`; scoreboard records residual deassembly_fifo entries as terminate-aborted drops |
| X046 | D | planned | none | TERMINATE with an in-flight CSR read of FILL_LEVEL — read must return an in-range fill-level reflecting the drain progress | `FILL_LEVEL` read value monotonically non-increasing during TERMINATING; no underflow (`proc_fill_level_meter` clamps to 0 when push=pop=overwrite=0) |
| X047 | D | planned | none | TERMINATE while a PUSH_COUNT CSR write is in flight — write to POP/PUSH counters is a no-op in `proc_csr_rw`; run-state transition completes unhindered | counter values unchanged by the write; `run_state_cmd` decode normal |
| X048 | D | planned | none | TERMINATE with `asi_ctrl_data=0x010` ANDed with another bit (`0x018`) — falls through to ERROR decode in `proc_run_control_mgmt` case | `run_state_cmd`=ERROR observable; asi_ctrl_ready stays low (`when others`); no state corruption |
| X049 | D | live UVM | none | TERMINATE→TERMINATE→IDLE sequence — first terminate ready, second redundant ack, then IDLE enters cleanly | asi_ctrl_ready pulses on each transition; final run_state_cmd=IDLE observable via run-state observer |
| X050 | D | planned | none | TERMINATE observed mid-`proc_fill_level_meter` update (push_cnt-pop_cnt-overwrite_cnt subtraction crossing underflow guard) | `FILL_LEVEL` CSR never reads a negative/all-ones value; explicit underflow guard in RTL keeps the CSR at 0 at exact balance |
| X051 | D | planned | none | FLUSH equivalent (RUN_PREPARE entry) with empty ring-buffer — `pop_engine_state` goes IDLE→FLUSHING→FLUSHING_RST then run_mgmt_flushed=1 | asi_ctrl_ready asserts when cam_clean=1 and both fifos empty; `FILL_LEVEL`=0; counters OVERWRITE/CACHE_MISS/INERR reset to 0 (flushing branch) |
| X052 | D | planned | none | FLUSH with full ring (`FILL_LEVEL=RING_BUFFER_N_ENTRY`) — FLUSHING must zero every CAM cell and every side RAM cell before run_mgmt_flushed | `FILL_LEVEL`=0 post-flush; `OVERWRITE_COUNT`=0 (cleared by decision_reg=3 branch); no residual hit drained afterward |
| X053 | D | live UVM | none | FLUSH during active drain (pop_engine_state=DRAIN when RUN_PREPARE arrives) — drain must be preempted by FLUSHING transition | no further aso_hit_type2_valid beats after FLUSHING enters; pop_cmd_fifo_sclr=1 clears pending commands; scoreboard: drain-aborted classification for residents |
| X054 | D | planned | none | FLUSH interleaved with pushes arriving (hits at deassembly input while RUN_PREPARE asserted) — proc_avst_input_deassembly_comb gates wrreq because run_state_cmd≠RUNNING | `PUSH_COUNT` unchanged during FLUSHING; arriving hits silently dropped at deassembly stage; no error count side-effect |
| X055 | D | live UVM | none | FLUSH with ERROR injection pending (asi_hit_type1_error=1) concurrent with RUN_PREPARE — filter path in `proc_csr_rw` clears `inerr_cnt` in the decision_reg=3 branch regardless | `INERR_COUNT` = 0 at end of FLUSHING; pre-flush count is wiped |
| X056 | D | planned | none | FLUSH followed by immediate new-key push once RUNNING entered — first GOOD push after flush must see `FILL_LEVEL`=0 and normal push latency | `PUSH_COUNT` += 1; `FILL_LEVEL`=1; ingress-to-CAM latency matches `EXPECTED_LATENCY` CSR value within tolerance |
| X057 | D | planned | none | FLUSH across partition boundary — CAM flush walks flush_cam_wraddr across all `POP_FLUSH_CAM_ADDR_MAX` addresses, incrementing flush_cam_wrdata 0→`POP_FLUSH_CAM_DATA_MAX` per address | pop_flush_cam_done asserts only after full 2D walk; `FILL_LEVEL`=0; no stale CAM match on any sk post-flush |
| X058 | D | planned | none | FLUSH without prior TERMINATE (RUN_PREPARE entered from RUNNING directly) — legal per RTL; `endofrun_seen` is cleared on RUN_PREPARE entry; residents are lost | asi_ctrl_ready asserts after flush completes; scoreboard classifies residents as terminate-aborted drain |
| X059 | D | planned | none | FLUSH clearing stale epoch: first FLUSH then pushes on a new sk that existed before FLUSH — new pushes must use a clean CAM with no stale occupancy bit | `PUSH_COUNT` += N; `OVERWRITE_COUNT` = 0 (fresh addresses); `CACHE_MISS_COUNT` = 0 |
| X060 | D | planned | none | FLUSH with a backlog in `deassembly_fifo` — fifo sclr is asserted by `deassembly_fifo_sclr<='1'`; entries drop silently | no PUSH events during FLUSHING; deassembly_fifo_empty=1 observable in scoreboard |
| X061 | D | live UVM | none | FLUSH with a backlog in `pop_cmd_fifo` — fifo sclr asserted; pending pop commands lost; `POP_COUNT` freezes | `POP_COUNT` unchanged during FLUSHING; scoreboard: all pending pop commands classified as flush-aborted |
| X062 | D | planned | none | FLUSH sequence where `run_mgmt_flush_memory_start` is already high from a previous RUN_PREPARE still being ack'd — latch prevents double trigger | pop_engine_state stays in FLUSHING until pop_flush_cam_done AND pop_flush_ram_done; run_mgmt_flushed asserts once |
| X063 | D | planned | none | FLUSH where `pop_flush_grant` is denied one cycle by the arbiter in favor of a push_erase_req — grant mask (req(3) has highest priority per `if req(3)='1' then ...`) — verify flush actually wins | `pop_flush_grant`=1 sampled, push_write_grant=0 during FLUSHING; scoreboard: no stray push event logged |
| X064 | D | planned | none | FLUSH with a CSR read of CACHE_MISS_COUNT mid-flush — read returns live counter which is being reset in the same cycle; contract: RO read returns the eventually-stabilized zero | `CACHE_MISS_COUNT`=0 after run_mgmt_flushed=1; intermediate read may see non-zero but must stabilize within 3 clk |
| X065 | D | planned | none | FLUSH followed by TERMINATE without going through RUNNING — TERMINATE ack depends on `terminating_drain_done`, which is trivially true if no residents | asi_ctrl_ready asserts within 2 clk of TERMINATE valid; no stray drain beats |
| X066 | D | planned | none | FLUSH with concurrent asi_ctrl_valid pulses from run-control (back-to-back RUN_PREPARE) — second RUN_PREPARE is a no-op while run_mgmt_flushed still asserted | asi_ctrl_ready asserts exactly once per completed flush; no restart of flush walk |
| X067 | D | planned | none | FLUSH completion coincident with a fresh asi_ctrl_valid=SYNC — SYNC case in `proc_run_control_mgmt` asserts gts_counter_rst; ack immediate | `gts_end_of_run` unchanged (only TERMINATE triggers it); asi_ctrl_ready=1 on SYNC within 1 clk |
| X068 | D | planned | none | FLUSH initiated while `endofrun_seen`=1 — endofrun_seen cleared on RUN_PREPARE entry (`run_state_cmd_next_v = IDLE or RUN_PREPARE` branch) | endofrun_seen=0 observable after RUN_PREPARE decode |
| X069 | D | planned | none | FLUSH timing: verify run_mgmt_flushed latch holds across a cycle when pop_cmd_fifo_empty lags cam_clean by 1 — Revision 2.7 fix | asi_ctrl_ready does not glitch low before rising; scoreboard sees exactly one RUN_PREPARE ack pulse |
| X070 | D | planned | none | FLUSH with `DEBUG=0` parameterization — ensure flush path is not gated by DEBUG (DEBUG only controls optional diagnostics, not functional flush) | run_mgmt_flushed timing identical to DEBUG=1 case within 1 clk |
| X071 | D | planned | none | soft-reset pulse (`csr.soft_reset<=1` via CTRL bit1 write) during RUNNING with traffic — RTL note says "TODO: implement this" — case is a forward-looking placeholder, for now verify the bit self-clears | `CTRL` readback bit1 returns to 0 within 2 clk; no functional side-effect on counters or `FILL_LEVEL` in current revision |
| X072 | D | planned | none | soft-reset in RUN_PREPARE — CTRL write during FLUSHING must not interfere with flush handshake | run_mgmt_flushed still asserts normally; CTRL readback bit1=0; asi_ctrl_ready asserts as usual |
| X073 | D | planned | none | soft-reset in TERMINATING — CTRL write during drain; ensure drain completion path unchanged | `POP_COUNT` completes normally; `terminating_drain_done` unaffected |
| X074 | D | planned | none | soft-reset during active drain (pop_engine_state=DRAIN) — placeholder for future clear semantics | drain continues to eop; no anomalous aso_hit_type2_error |
| X075 | D | planned | none | soft-reset concurrent with a CSR write of EXPECTED_LATENCY — CSR write arbitration: the write acknowledgement path is mutually exclusive per `proc_csr_rw`; both writes complete in order | both CSR values reflect their last write; no waitrequest deadlock |
| X076 | D | planned | none | soft-reset expected to clear INERR_COUNT (forward-looking): inject 4 bad hits, assert soft_reset, expect `INERR_COUNT`=0 | when implemented: `INERR_COUNT`=0 within 3 clk; current revision: retained (case marked placeholder until RTL change) |
| X077 | D | planned | none | soft-reset expected to clear PUSH_COUNT — inject pushes, soft-reset, expect 0 | when implemented: `PUSH_COUNT`=0; current revision: retained |
| X078 | D | planned | none | soft-reset expected to clear POP_COUNT — drain some hits, soft-reset, expect 0 | when implemented: `POP_COUNT`=0; current revision: retained |
| X079 | D | planned | none | soft-reset expected to clear OVERWRITE_COUNT — force overwrites, soft-reset, expect 0 | when implemented: `OVERWRITE_COUNT`=0; current revision: retained (flushing path is the only clearer today) |
| X080 | D | planned | none | soft-reset expected to clear CACHE_MISS_COUNT — force cache misses, soft-reset, expect 0 | when implemented: `CACHE_MISS_COUNT`=0; current revision: retained |
| X081 | D | planned | none | soft-reset expected to clear FILL_LEVEL — push hits, soft-reset, expect 0 | when implemented: `FILL_LEVEL`=0 without requiring drain; current revision: unchanged |
| X082 | D | live UVM | none | soft-reset with self-clearing bit: confirm CTRL bit1 reads back 0 after a single write of 1, without a follow-up write of 0 | CTRL bit1 read = 0 within 2 clk of the write handshake |
| X083 | D | live UVM | none | soft-reset with CTRL bit1 held at 1 across multiple writes — proc_csr_rw assigns 0 unconditionally in the idle branch; confirm no latch-up | CTRL bit1 returns to 0 every cycle with no write in flight |
| X084 | D | live UVM | none | back-to-back soft-resets (two CTRL writes with bit1=1 in consecutive cycles) — both writes must ack; bit1 returns to 0 after each | avs_csr_waitrequest handshake completes twice; CTRL bit1 readback=0 |
| X085 | D | planned | none | soft-reset during a CSR read of UID (address 0) — read must return the IP_UID constant unaffected | UID read = `IP_UID` generic; no read corruption |
| X086 | D | planned | none | illegal CTRL write: bit2 (reserved) set to 1 — only bits {1=soft_reset, 4=filter_inerr} are defined; reserved bits must be inert | CTRL readback shows only bits 1 and 4; reserved bits read back 0 |
| X087 | D | planned | none | illegal CTRL write: bit3 (reserved) set to 1 — inert | CTRL readback: bit3=0 |
| X088 | D | planned | none | illegal CTRL write: upper 27 bits set to all-1 — only bits 1 and 4 latch | CTRL readback = `0x00000012` regardless of input 0xFFFFFFFF |
| X089 | D | live UVM | none | write to RO register UID (address 0) — `proc_csr_rw` when-branch has no write case for UID; should be inert | UID readback unchanged; avs_csr_waitrequest handshake completes |
| X090 | D | planned | none | write to RO register META (address 1) — inert | META readback unchanged (version/build constants) |
| X091 | D | live UVM | none | write to RO register FILL_LEVEL (address 4) — inert | FILL_LEVEL readback reflects live counter, not written value |
| X092 | D | live UVM | none | write to RO register PUSH_COUNT (address 6) — inert (no write side-effect) | PUSH_COUNT unchanged by write |
| X093 | D | live UVM | none | write to RO register POP_COUNT (address 7) — inert | POP_COUNT unchanged |
| X094 | D | live UVM | none | write to RO register OVERWRITE_COUNT (address 8) — commented-out clear path; write is currently inert | OVERWRITE_COUNT unchanged; no inadvertent clear |
| X095 | D | live UVM | none | write to RO register CACHE_MISS_COUNT (address 9) — inert per when-others | CACHE_MISS_COUNT unchanged |
| X096 | D | live UVM | none | write to unmapped CSR offset (address 10..31 within 5-bit address space) — when-others falls through with no effect; waitrequest handshake still completes | no counter changes; readback from unmapped offset returns default/previous |
| X097 | D | live UVM | none | run-control word = undefined value (e.g. `0x0FF`, multi-hot) — `proc_run_control_mgmt` falls through to `run_state_cmd_next_v := ERROR` | run_state_cmd=ERROR observable; `asi_ctrl_ready=0` per when-others branch; no state corruption |
| X098 | D | live UVM | none | run-control word = 0x000 (all zeros) — hits the when-others branch → ERROR | run_state_cmd=ERROR; asi_ctrl_ready=0 |
| X099 | D | live UVM | none | run-control command TERMINATE sent while already in IDLE (no prior RUNNING) — TERMINATING entered from a quiescent empty state; ack immediate without waiting for `endofrun_seen` | asi_ctrl_ready within 2 clk; no spurious drain beats |
| X100 | D | live UVM | none | LINK_TEST command (0x020) — `when others` branch in main state machine → asi_ctrl_ready=0 (not supported yet) | asi_ctrl_ready stays 0; no state change observable on output streams |
| X101 | D | planned (requires backdoor counter observer) | none | single bad-hit injection — measure max cycle delay from `asi_hit_type1_error(0)=1` to `INERR_COUNT` incrementing on the CSR frontdoor | backdoor observer samples `debug_msg2.inerr_cnt` change within 1 clk of the bad beat; frontdoor CSR read ≤ 3 clk after via avs_csr handshake |
| X102 | D | planned (requires backdoor counter observer) | none | burst of 16 bad hits in 16 consecutive cycles — confirm no counter updates are lost under back-to-back events | backdoor `inerr_cnt` = 16 within 1 clk of the 16th bad beat; frontdoor `INERR_COUNT` = 16 within 4 clk |
| X103 | D | planned (requires backdoor counter observer) | none | bad hit near-simultaneous with a good push — both inerr_cnt and push_cnt increments must be observable on the same cycle at the backdoor | backdoor: `inerr_cnt` +=1 AND `push_cnt` +=1 within 1 clk; frontdoor CSR reads agree within 4 clk |
| X104 | D | planned (requires backdoor counter observer) | none | push event near TERMINATE — verify `PUSH_COUNT` updates before TERMINATING freezes deassembly | backdoor `push_cnt` captures the final push ≤1 clk before terminating_drain_done; frontdoor `PUSH_COUNT` read after terminate matches |
| X105 | D | planned (requires backdoor counter observer) | none | pop event near TERMINATE — drain retires the last resident; `POP_COUNT` backdoor update must precede asi_ctrl_ready by ≥1 clk | backdoor `pop_cnt` final update 1 clk before asi_ctrl_ready; frontdoor `POP_COUNT` stable after terminate |
| X106 | D | planned (requires backdoor counter observer) | none | overwrite event near TERMINATE — force a collision on the last-cycle-before-TERMINATE push | backdoor `overwrite_cnt` +=1 within 1 clk of collision; frontdoor `OVERWRITE_COUNT` read post-terminate matches |
| X107 | D | planned (requires backdoor counter observer) | none | cache-miss event near TERMINATE — force a drain where `side_ram_dout(high)=0` during the last residents' pop | backdoor `cache_miss_cnt` +=1 within 1 clk of pop_cache_miss_pulse; frontdoor `CACHE_MISS_COUNT` read post-terminate matches |
| X108 | D | planned (requires backdoor counter observer) | none | inerr event near FLUSH — bad hit one cycle before RUN_PREPARE asserted — backdoor must see inerr_cnt go to 0 after FLUSHING decision_reg=3 branch | backdoor: `inerr_cnt` +=1 then =0 within the FLUSHING window; frontdoor reads 0 after run_mgmt_flushed |
| X109 | D | planned (requires backdoor counter observer) | none | overwrite event near FLUSH — force overwrite one cycle before RUN_PREPARE | backdoor: `overwrite_cnt` +=1 then =0; frontdoor reads 0 after flush |
| X110 | D | planned (requires backdoor counter observer) | none | cache-miss event near FLUSH — force cache miss one cycle before RUN_PREPARE | backdoor: `cache_miss_cnt` +=1 then =0; frontdoor reads 0 after flush |
| X111 | D | planned (requires backdoor counter observer) | none | push event near FLUSH — push one cycle before RUN_PREPARE; `push_cnt` is NOT cleared by decision_reg=3 in current RTL (only overwrite/cache_miss/inerr are) | backdoor `push_cnt` retains value through FLUSHING; frontdoor `PUSH_COUNT` unchanged |
| X112 | D | planned (requires backdoor counter observer) | none | pop event near FLUSH — pop one cycle before RUN_PREPARE; `pop_cnt` retained | backdoor `pop_cnt` retained; frontdoor `POP_COUNT` unchanged |
| X113 | D | planned (requires backdoor counter observer) | none | inerr event coincident with soft-reset write — verify expected clear semantics (forward-looking) vs current retention | current: `INERR_COUNT` retained; future-implemented: `INERR_COUNT`=0 within 3 clk |
| X114 | D | planned (requires backdoor counter observer) | none | single push at exact cycle of a CSR read of PUSH_COUNT — read must reflect either pre- or post-push value, never mid-update garbage | frontdoor `PUSH_COUNT` read ∈ {N, N+1}; backdoor confirms the exact cycle of increment |
| X115 | D | planned (requires backdoor counter observer) | none | verify 32-bit frontdoor truncation of the 48-bit internal `inerr_cnt` / `push_cnt` / `pop_cnt` / `overwrite_cnt` / `cache_miss_cnt` — backdoor observer reads 48 bits, frontdoor sees low 32 | if test forces >=2^32 events (long run), frontdoor wraps while backdoor continues monotonically; sanity: match on low-32 bits at sample points |
| X116 | D | live UVM | none | GOOD → ERROR → RECOVERY: run 32 GOOD pushes, inject 8 bad hits, resume 32 GOOD pushes; all 64 GOOD references must drain; 8 errors must be isolated | `PUSH_COUNT`=64; `INERR_COUNT`=8; `POP_COUNT`=64 after TERMINATE; `FILL_LEVEL`=0; scoreboard: zero unexpected drops, zero unexpected duplicates |
| X117 | D | live UVM | none | GOOD → ERROR burst → FLUSH → GOOD: verify FLUSH cleans the error epoch (counters zeroed) and the second GOOD burst sees fresh state | after FLUSH: `INERR_COUNT`=0, `OVERWRITE_COUNT`=0, `CACHE_MISS_COUNT`=0; after second GOOD burst: `PUSH_COUNT` = only the second-burst count; `FILL_LEVEL` = second-burst residents |
| X118 | D | live UVM | none | GOOD → TERMINATE mid-traffic → RUN_PREPARE → RUN → GOOD: full restart cycle; `endofrun_seen` cleared on RUN_PREPARE | first-burst residents classified terminate-aborted drain; second-burst fully drained on its own TERMINATE; `FILL_LEVEL`=0 at end |
| X119 | D | planned | none | GOOD → overwrite-heavy ERROR window (same-key collisions) → recovery with TERMINATE — confirm overwrite-loss is accounted and next run is unaffected | `OVERWRITE_COUNT` equals actual collision count; scoreboard: every GOOD reference is either drained or expected-overwrite; recovery run has `OVERWRITE_COUNT` reset to 0 (after next FLUSH) |
| X120 | D | planned | none | GOOD → cache-miss heavy window → recovery — drain on an empty CAM slot (side_ram occupancy=0) bumps `CACHE_MISS_COUNT`; recovery run sees 0 | `CACHE_MISS_COUNT` increments per pop_cache_miss_pulse; post-FLUSH value=0; scoreboard: cache-miss beats carry the appropriate aso_hit_type2_error bit from the ts[12] check |
| X121 | D | planned | none | recovery invariant: after any ERROR→FLUSH→RUN transition, the first N pushes must have latency identical to a cold-start N-push sequence (within EXPECTED_LATENCY tolerance) | latency of pushes 1..N matches cold-start reference within +/-2 clk; `EXPECTED_LATENCY` CSR unchanged |
| X122 | D | planned | none | recovery invariant: after recovery, `UID` and `META` CSRs return the generic-wired constants bit-for-bit | `UID` = `IP_UID`; `META` packs version major/minor/patch/build/date per `proc_csr_rw`; no corruption |
| X123 | D | planned | none | recovery invariant: after recovery, the run-state observer sees a legal IDLE→RUN_PREPARE→SYNC→RUNNING→TERMINATING→IDLE sequence with ack timing matching a fresh IP | each asi_ctrl_ready pulse timing within 1 clk of a reference fresh run |
| X124 | D | planned | none | recovery invariant: after a forced underflow guard trip (pop>push+overwrite scenario impossible in normal flow but TB can synthesize via soft-reset asymmetry), `FILL_LEVEL` CSR still reads 0 (not all-ones) | `FILL_LEVEL`=0 via the `push=pop=overwrite=0` guard in `proc_fill_level_meter` |
| X125 | D | planned | none | recovery invariant: after any ERROR scenario, `aso_filllevel_data` streaming output (16-bit fill-level side band) tracks `FILL_LEVEL(15:0)` exactly | `aso_filllevel_data` == `csr.fill_level(15:0)` every valid beat |
| X126 | D | live UVM | none | recovery invariant: after ERROR→FLUSH→RUN, a subsequent TERMINATE with no residents trivially acks (`terminating_drain_done` = 1 immediately) | asi_ctrl_ready in TERMINATING within 2 clk of valid |
| X127 | D | live UVM | none | recovery invariant: `endofrun_seen` flag is cleared across the full IDLE/RUN_PREPARE boundary; second run of the same test case does not inherit the flag | endofrun_seen observable=0 at RUNNING entry of second run |
| X128 | D | live UVM | none | recovery invariant: `gts_end_of_run` snapshot captures a new timestamp on each TERMINATE entry (the RTL latches it only on the IDLE→TERMINATING edge) | `gts_end_of_run` value differs between two runs by at least the inter-run gap in gts_8n units |
| X129 | D | planned | none | recovery invariant, full signoff: after GOOD → mixed ERROR (bad-hit + overwrite + cache-miss) → FLUSH → GOOD, the scoreboard reports: zero unexpected drops, zero unexpected duplicates, all error-window events isolated to their window, all good references drained with expected latency | scoreboard summary all-green: `INERR_COUNT`/`OVERWRITE_COUNT`/`CACHE_MISS_COUNT` after flush=0; `PUSH_COUNT`/`POP_COUNT` of recovery run match exactly; `FILL_LEVEL`=0 at end-of-test |
