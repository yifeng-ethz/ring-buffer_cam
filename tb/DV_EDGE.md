# DV_EDGE.md — ring_buffer_cam

**Companion to:** [DV_PLAN.md](DV_PLAN.md), [DV_HARNESS.md](DV_HARNESS.md)
**Canonical ID Range:** `E001-E129`
**Intent:** boundary, timing, and arbitration corners for ring_buffer_cam contracts.

| case_id | method | implementation | legacy alias | scenario | primary checks |
|---|---|---|---|---|---|
| E001 | D | live UVM | old `D002` intent | zero-hit subheader emission for searched key with no resident hits | SOP/EOP zero-hit framing and cache-miss counting behavior |
| E002 | D | live UVM | old `D003` intent | max-depth same-key burst at CAM boundary | boundary drain count and no off-by-one corruption |
| E003 | D | live UVM | `test_overwrite_stress` | same-ts hotspot burst that injects more than 512 hits into one sector before the `EXPECTED_LATENCY=2000` service window can retire them | legal overwrite lower bound is derived from pushes observed before first pop service; no unexplained loss beyond overwrite accounting |
| E004 | D | live UVM | old `D021` intent | push traffic arrives while pop drain is active | arbitration between push/pop keeps integrity |
| E005 | D | live UVM | old `D011` intent | consecutive data-bearing subheaders with minimal gap | back-to-back drains preserve search-key framing even with zero-hit subheaders interleaved |
| E006 | D | live UVM | old `D023-D024` intent | same-key burst spans beyond one promoted-config partition while service is held off | partitions share storage without premature overwrite; post-terminate drain remains lossless |
| E007 | D | live UVM | old `D029` intent | one partition empties while another still has one remaining hit | drain continues across the partition boundary with no global stall |
| E008 | D | live UVM | old `D008` intent | minimum and maximum expected-latency settings | latency programming changes behavior without corruption |

# Canonical Planned Cases (E009-E129)

| case_id | method | implementation | legacy alias | scenario | primary checks |
|---|---|---|---|---|---|
| E009 | D | live UVM | none | fill to exactly RING_BUFFER_N_ENTRY-1 (511) same-key hits, then drain | FILL_LEVEL = N-1, PUSH_COUNT = 511, POP_COUNT = 511 after drain; no `push_state=ERASE` entry (no overwrite) catches premature overwrite at N-1 |
| E010 | D | live UVM | none | fill to exactly RING_BUFFER_N_ENTRY (512) same-key hits with no drain window yet | `write_pointer` wraps from 511 back to 0; FILL_LEVEL = 512; OVERWRITE_COUNT still 0; catches write_pointer wrap miscount or spurious ERASE on unoccupied slot 0 |
| E011 | D | live UVM | none | fill to RING_BUFFER_N_ENTRY+1 (513) same-key hits; first wrap-write lands on slot 0 which was already occupied | `push_state` transitions WRITE_AND_CHECK -> ERASE on beat 513 because `addr_occupied='1'` on slot 0; OVERWRITE_COUNT=1, PUSH_COUNT=512, FILL_LEVEL=512; catches wrap-induced overwrite not firing |
| E012 | D | live UVM | none | fill to 2*RING_BUFFER_N_ENTRY (1024) same-key hits through two full wraps | OVERWRITE_COUNT = 512, PUSH_COUNT = 512 (never drops since overwrite + push are counted via `decision_reg=0` and `decision_reg=1` separately); FILL_LEVEL stays at 512 throughout; catches counter drift over repeated wrap |
| E013 | D | live UVM | none | fill exactly to one-hot partition boundary minus one resident for the promoted config | subheader hit_count field reports the pre-boundary occupancy exactly; catches off-by-one on partition slicing of `cam_match_addr_oh_partitioned_comb` |
| E014 | D | live UVM | none | fill exactly to one full partition span for the promoted config | boundary occupancy still drains losslessly with the expected hit_count field; catches boundary-bit slicing errors |
| E015 | D | live UVM | none | fill one resident beyond a full partition span for the promoted config | long same-key drain packetizes into multiple data-bearing subheaders while remaining lossless and preserving search key |
| E016 | D | live UVM | none | fill the full ring depth with one same-key hotspot | full-depth drain packetizes across multiple data-bearing subheaders while remaining lossless and preserving search key |
| E017 | D | live UVM | none | wrap index: write_pointer rolls from MAIN_CAM_ADDR_WIDTH max (511) to 0 with a pending pop on slot 510 | check pop_erase at slot 510 races with push_write at slot 511 then 0; `decision` arbitration 0 vs 2 yields pop_erase=2 when `pop_engine_state/=IDLE`; catches push_write leaking into pop-claimed slot |
| E018 | D | planned | none | first-in-first-out at row boundary: write slots 510, 511, 0 same key; drain via pop, observe issue order | `pop_rr_idx` visits partition 3 (slots 384..511) first then partition 0 (0..127) per round-robin; verify binary LSB encoding of slot 510 (=partition3 LSB 126) precedes slot 0; catches LSB encoder direction |
| E019 | D | live UVM | none | depth-1 (511 hits) same-key while EXPECTED_LATENCY=min (1) | latency register reaches `1`, first pop descriptor appears quickly, and the full 511-hit epoch drains losslessly with no overwrite; catches latency-1 underflow / early-drain path regressions |
| E020 | D | live UVM | none | depth (512 hits) same-key with EXPECTED_LATENCY=max (0xFFFF=65535) | no descriptor or subheader appears in the early window, first pop is delayed by ~65k cycles, and the full 512-hit epoch still drains losslessly; catches latency-max premature-pop bugs |
| E021 | D | live UVM | none | push 512 hits across all 64 lane-local search keys admitted by INTERLEAVING_FACTOR=4 (8 hits per key) | each admitted key emits one data-bearing subheader, total push/pop is 512/512 with no overwrite, and the lane-local key sweep closes the full admitted-key space without deassembly stalls |
| E022 | D | planned | none | push exactly 16 hits into pop_cmd_fifo (POP_CMD_FIFO depth) before first drain | `pop_cmd_fifo_usedw` reaches 2^4-1=15, `pop_cmd_fifo_full='1'`; descriptor generator should stall; catches missing full-check in `proc_pop_descriptor_generator` dropping 17th command silently |
| E023 | D | live UVM | none | run a 2000-hit lane-local cyclic stream at one hit every ~32 cycles with EXPECTED_LATENCY=2000 | push/pop closes at 2000/2000 with no overwrite and the modeled backlog stays in a bounded steady-state window (`max_remaining` between 32 and 128); catches long-run drain/accounting hangs and scoreboard slot-reuse blind spots |
| E024 | D | planned | none | push hits such that only the last row of partition 3 (slot 511) is occupied | `cam_match_addr_oh_partitioned_comb(3)` bit 127 asserted; encoder `result_lsb_reg` = 127; binary issue addr = 3*128+127 = 511; catches partition-index-to-address formula `pop_rr_idx * MATCH_PARTITION_SIZE_CONST + addr_lsb` arithmetic overflow |
| E025 | D | live UVM | none | push hits such that only slot 0 of partition 0 is occupied | encoder LSB=0 immediate, `pop_partition_flag(0)=1`; issue addr=0; all other partitions' `pop_partition_pending` stay 0; catches spurious pending bits leaking from stale snapshot |
| E026 | D | live UVM | none | push hits such that partition 0 and the last configured partition hold matches, intermediates empty | round-robin starts pop_rr_idx=0, drains P0, advances through empty partitions until last partition; verify no stall-cycle waste beyond the one-cycle `next_rr_idx_v` skip; catches encoder not auto-skipping empty partitions |
| E027 | D | planned | none | push hits populating partition 2 only while search key matches partition 2's slots | encoder on partition 0,1,3 reports `o_result_valid=1, o_cam_match_flag=0`; DRAIN state only issues from partition 2; catches erroneous pop_issue from a partition with flag=0 |
| E028 | D | planned | none | latency boundary: EXPECTED_LATENCY programmed at depth-1 (N-1 = 511) cycles | pop pointer trails push pointer by exactly 511; at CAM full moment, last push slot is being erased by pop_erase on same cycle; catches `pop_erase_grant` colliding with occupied slot flag write-back |
| E029 | D | planned | none | partition-aligned stride: fill with keys ts[11:4] = 0x00, 0x20, 0x40, 0x60 to bucket by partition via modulo | verify INTERLEAVING_INDEX matching still holds; `pop_cmd_fifo_din = read_time_ptr[12:4]` correct for each bucket; catches interleaving filter dropping wrong modulo |
| E030 | D | planned | none | depth boundary with concurrent deassembly_fifo near-full (usedw ~ 255) | `asi_hit_type1_ready` drops on `deassembly_fifo_full='1'`; backpressure propagates up; verify no internal counter increments during backpressure; catches PUSH_COUNT ticking on `decision_reg=0` while no real data moved |
| E031 | D | live UVM | none | subheader SOP-only zero-hit drain: search key with zero matches | `pop_count_total_acc=0` in COUNT, pop_engine_state→RESET, `aso_hit_type2_startofpacket=1` AND `aso_hit_type2_endofpacket=1` on same beat (SOP+EOP zero-hit frame); catches EOP missing on zero-hit |
| E032 | D | live UVM | none | subheader SOP with N=1 hit: exactly one match in partition 0 | SOP on subheader beat (no EOP), next beat hit with EOP on `pop_last_hit_pending=1`; verify two distinct beats; catches EOP placement on subheader vs on last hit |
| E033 | D | live UVM | none | subheader SOP with N=2 hits, both in same partition 0 adjacent slots | SOP beat, hit0 beat, hit1 beat with EOP; encoder issues slot X, advances, issues slot X+1; catches encoder `i_advance` failing to refresh LSB between back-to-back issues |
| E034 | D | planned | none | subheader SOP with N=2 hits split across partition 0 and partition 2 | round-robin picks P0 first then P2; hit order P0-hit then P2-hit; verify `pop_issue_partition_idx` transitions 0→2 not 0→1→2 cycle-by-cycle stall; catches wrong fall-through on empty interstitial partitions |
| E035 | D | live UVM | none | subheader hit_cnt field boundary at value 255 (8-bit saturation) | send 255 same-key hits; subheader [15:8]=0xFF; verify drain of all 255 though field only exposes low 8 bits of `pop_total_hits(7:0)`; catches silent truncation when real hits > 255 |
| E036 | D | live UVM | none | subheader hit_cnt field at value 256 (9-bit needed but only 8 exposed) | send 256 same-key hits; subheader [15:8]=0x00; verify actual drain count=256 via POP_COUNT CSR; catches discrepancy between exposed hit_cnt and real drained count |
| E037 | D | live UVM | none | subheader hit_cnt field at value 511 (max N-1) | subheader [15:8]=0xFF (low byte of 511); POP_COUNT=511; catches host code trusting subheader hit_cnt when >255 |
| E038 | D | planned | none | back-to-back subheaders with zero idle gap between `pop_engine_state=RESET` exits | two pop commands in cmd_fifo; observe IDLE->SEARCH latency=1 cycle with showahead read, no wasted cycles; catches extra latency from `pop_cmd_fifo_rdack` handshake |
| E039 | D | planned | none | same-key-then-different-key with minimum 16-cycle gap (one descriptor period) | descriptor N, then N+1 generated 16 cycles apart; `pop_current_sk` updates from old to new exactly on second IDLE→SEARCH; catches stale `pop_current_sk` used for second subheader |
| E040 | D | planned | none | key change during in-flight DRAIN: second descriptor arrives while first is draining | `pop_cmd_fifo_empty='0'` during DRAIN, but `pop_cmd_fifo_rdack` only fires in RESET or when `pop_hits_count=1`; verify second search key waits until first drain fully ends; catches premature sk switch |
| E041 | D | planned | none | drain after `pop_engine_state=SEARCH` exit: 4-cycle `pop_search_wait_cnt` race with new push on same key | during the 5-cycle search wait, push_write is allowed (decision=0) — the new hit must NOT be included in the current search result since `pop_partition_snapshot` latches at end of SEARCH; catches snapshot refreshed after late push |
| E042 | D | planned | none | out_valid re-assertion after zero-hit: next drain starts on next IDLE with no dead cycle | zero-hit case at cycle C ends in RESET, IDLE at C+1, new SEARCH at C+2; `aso_hit_type2_valid` must re-assert on new subheader within 8 cycles (SEARCH wait + LOAD + COUNT); catches valid dropping longer |
| E043 | D | planned | none | out_valid pulses exactly one cycle per subheader beat (no valid hold on pop-side backpressure off) | with `aso_hit_type2_ready=1` forever, each beat is one cycle of valid; no double-pulse; catches valid staying high across two beats |
| E044 | D | planned | none | subheader emitted while `pop_pipeline_start=1` and `subheader_gen_done=0` — exactly one cycle overlap | on the beat `pop_pipeline_start` rises, subheader fires and `subheader_gen_done<='1'`; next beat cannot re-emit even though `pop_pipeline_start=1` still holds; catches duplicate subheader |
| E045 | D | planned | none | zero-hit subheader fires on `pop_cmd_fifo_rdack='1'` (from COUNT→RESET transition with total_acc=0) | the single beat has SOP+EOP, hit_cnt=0; `subheader_gen_done` is armed then cleared in next IDLE; catches path that needs `pop_pipeline_start` to be high first |
| E046 | D | planned | none | pop_last_hit_pending timing: last drained hit has EOP in same cycle as pop_hits_count transitions 1→0 | verify `pop_last_hit_pending<='1'` on beat where `pop_hits_count=1` AND `pop_erase_grant=1`; EOP shows next cycle when hit data emits; catches EOP off-by-one |
| E047 | D | planned | none | tsglitcherr: push a hit where ram[12] differs from pop_current_sk[8] | drain that hit, `aso_hit_type2_error(0)` asserts for exactly that beat; other beats clear; catches error latched or leaking to subsequent hits |
| E048 | D | planned | none | minimum descriptor gap: interleaving bits force 16-cycle cadence | `read_time_ptr(3:0)=0000` AND `read_time_ptr(TCC8N_INTERLEAVING_HI:4)=INTERLEAVING_INDEX`; verify exactly 1 wrreq per 16-cycle window; catches descriptor emitted twice in a window |
| E049 | D | planned | none | interleaving mismatch: send hit with TCC8N_INTERLEAVING bits NOT equal to INTERLEAVING_INDEX | `deassembly_fifo_wrreq='0'`; PUSH_COUNT does not advance; catches interleaving filter letting wrong lane in |
| E050 | D | planned | none | `asi_hit_type1_empty=1` channel-targeted close marker | path takes channel-bits branch (`asi_hit_type1_channel(1:0)`) not data-bits branch; `deassembly_fifo_wrreq='0'` regardless of match; `endofrun_seen` only fires if channel matches INTERLEAVING_INDEX; catches close-marker written as a hit |
| E051 | D | planned | none | cache-miss on empty CAM: send search descriptor before any push | `pop_partition_pending=0000`, `pop_count_total_acc=0`, COUNT→RESET with rdack; `aso_hit_type2_startofpacket=1, endofpacket=1`, hit_cnt=0; CACHE_MISS_COUNT does NOT increment (pulse only fires in hit-emit branch); catches cache_miss being counted on empty-search |
| E052 | D | planned | none | cache-miss after `run_state_cmd=TERMINATING`: search on CAM emptied by drain | after terminate, one more descriptor fires zero-hit subheader; CACHE_MISS_COUNT stays 0 (same reason as E051); catches terminate leaving stale matches |
| E053 | D | planned | none | cache-miss after `run_state_cmd=RUN_PREPARE` flush: CAM flushed to all-1s via `pop_flush_cam_done` | searches on flushed CAM see no match because `cam_cmp_din` (low 8 bits) cannot match 0xFFFF_FFFF patterns; verify zero-hit subheaders; catches flush not writing FLUSH_MAX |
| E054 | D | planned | none | cache-miss on first cycle of `run_state_cmd=RUNNING`: `gts_counter_rst='0'` just released | descriptor generator inactive until `gts_8n >= expected_latency_48b`; verify first pop command at gts=2000 (default latency); catches premature descriptor at gts=0 |
| E055 | D | planned | none | cache-miss at exact cycle a push is writing the same key (push_write decision=0 during SEARCH wait) | push lands mid-SEARCH; snapshot latched at end of SEARCH, so new push is NOT in snapshot; zero-hit subheader still fires if only this push existed; catches race where snapshot captures mid-wait pushes |
| E056 | D | planned | none | cache-miss sequence miss/hit/miss on consecutive 16-cycle windows | 3 descriptors, windows 0/1/2 have 0/1/0 hits; verify `pop_cache_miss_pulse` (hit-emit branch) fires only on the single hit of window 1 when `side_ram_dout(high)=0` would indicate stale slot — NOT the case here since hit is real; CACHE_MISS stays 0; catches stale slot pulsing false miss |
| E057 | D | planned | none | cache-miss pulse condition: pop_hit emitted but `side_ram_dout(high)=0` (occupancy flag cleared between latch and read) | construct race where CAM match exists but ram slot was pop-erased; `pop_cache_miss_pulse='1'` on that hit beat; CACHE_MISS_COUNT increments by 1; catches the stale-match path |
| E058 | D | planned | none | back-to-back zero-hit windows for 32 consecutive 16-cycle periods | 32 SOP+EOP-only beats; POP_COUNT stays 0; PUSH_COUNT stays 0; pop_engine cycles IDLE→SEARCH→LOAD→COUNT→RESET 32 times without hitting DRAIN; catches state trap in COUNT waiting for non-existent hits |
| E059 | D | planned | none | single hit sandwiched by zero-hit windows on adjacent keys | keys K-1 (miss), K (hit), K+1 (miss); subheader sequence SOP+EOP (K-1), SOP, hit, EOP (K), SOP+EOP (K+1); catches subheader_gen_done not clearing between zero and non-zero |
| E060 | D | planned | none | cache-miss while `pop_cmd_fifo_full=1` (16 pending descriptors) | descriptor generator must stall; verify no `wrreq` asserts when full; catches overwriting fifo pointer |
| E061 | D | planned | none | cache-miss during partition encoder pipeline stall (PIPE_STAGES=4): load arrives during active evaluation | new `i_load` supersedes in-flight via `start_eval<=i_load|...`; previous result_valid forced to 0 on load; catches two-shot result where old LSB overrides new snapshot |
| E062 | D | planned | none | cache-miss when gts_8n just crossed expected_latency boundary (first valid read_time_ptr) | verify descriptor fires exactly at gts=2000 and `pop_cmd_fifo_din = 0x00` (ts[11:4]=0 included per rev 2.9 fix); catches first bin (ts[11:4]=0) dropped |
| E063 | D | planned | none | cache-miss exactly at gts_end_of_run captured in TERMINATING | `gts_end_of_run<=gts_8n` on transition into TERMINATING; verify no descriptor fires past this point via `terminating_drain_done`; catches descriptor leaking past EOR |
| E064 | D | planned | none | cache-miss when filter_inerr=1 drops an errored hit — subsequent pop sees no match on errored slot | `debug_msg2.inerr_cnt` increments; `deassembly_fifo_wrreq=0`; no push; later search for that key yields zero; catches errored hit silently entering CAM |
| E065 | D | planned | none | cache-miss after `pop_engine_state=FLUSHING_RST` exit: first descriptor post-flush | verify `pop_engine_state=IDLE` entry honors `run_mgmt_flush_memory_start=0`; first descriptor fires cleanly; catches FLUSHING_RST→IDLE leaving stale pop_pipeline_start |
| E066 | D | planned | none | sequential miss pattern across INTERLEAVING boundary (factor 4): keys 0x00, 0x04, 0x08 to same lane | all three descriptors pass interleaving match `read_time_ptr[TCC8N_INTERLEAVING_HI:4]=INTERLEAVING_INDEX`; verify three zero-hit subheaders with consecutive ts[11:4]; catches interleaving check applied to descriptor side |
| E067 | D | planned | none | zero-hit window then single-hit window with only 1 cycle between RESET and new SEARCH | pop_cmd_fifo retains second descriptor through RESET; showahead reads on IDLE entry; verify SEARCH re-enters in 1 cycle; catches fifo rdack latency swallowing a command |
| E068 | D | planned | none | cache-miss on the exact cycle `csr.go=0` is written (host halt mid-descriptor) | `csr.go=0` prevents new `deassembly_fifo_wrreq` but existing descriptors still drain; verify pop_engine finishes current subheader; catches go=0 aborting in-flight drain |
| E069 | D | planned | none | cache-miss while `terminating_drain_done=1` is latched: no new descriptor should fire in TERMINATING | verify `pop_cmd_fifo_wrreq=0` when `terminating_drain_done=1`; catches generator still firing past drain-done |
| E070 | D | planned | none | cache-miss while deassembly_fifo is full but pop_cmd_fifo is empty | pop descriptor generator still fires (independent of deassembly path); zero-hit subheaders continue at 16-cycle cadence; catches generator gated on input path |
| E071 | D | planned | none | first-ever overwrite from virgin reset: exactly fill to 512 + 1 more push on same key | `push_state=ERASE` fires on 513th push; `debug_msg2.overwrite_cnt=1`, `debug_msg2.push_cnt=512` (NOT 513 — push_cnt increments only on decision=0, erase uses decision=1); catches push_cnt counting erases |
| E072 | D | planned | none | overwrite at exact CAM-full boundary with pop engine idle (no contention) | `decision` goes 0→1 (push_write then push_erase); verify `write_pointer-1` is used as erase addr (line 1343); catches wrong erase address |
| E073 | D | planned | none | overwrite while DRAIN is backpressured (`aso_hit_type2_ready=0` held) | sink stalls; `pop_hit_valid` can still latch but output stays valid; `pop_engine_state=DRAIN` holds; new push can still land via `decision=0` while pop is search/count-only; verify overwrite counter increments correctly and no data loss in side_ram; catches push stealing pop-reserved arbiter slot |
| E074 | D | planned | none | overwrite across partition boundary: push lands on slot 127 then wraps to slot 0 (partition 0 both times) | both overwrites within same partition; `pop_partition_pending(0)` tracks both via `cam_match_addr_oh_partitioned_comb(0)`; catches partition slice mid-row miss |
| E075 | D | planned | none | overwrite exactly at boundary between partition 3 and partition 0: last write slot 511 then wrap slot 0 | slot 511 is P3 bit 127, slot 0 is P0 bit 0; overwrite on slot 0 must update P0's snapshot, not P3's; catches partition index computed from `cam_wr_addr` vs from `write_pointer` mismatch |
| E076 | D | planned | none | overwrite during `run_state_cmd=TERMINATING` with `endofrun_seen=1` | deassembly_fifo_wrreq=0 (ingress closed because `endofrun_seen=0` required); no new pushes, so no overwrites possible post-EOR; `debug_msg2.overwrite_cnt` frozen; catches EOR-past pushes creating overwrites |
| E077 | D | planned | none | overwrite on the CSR read beat of OVERWRITE_COUNT (host polling race) | read CSR addr 8 while overwrite increments; `avs_csr_readdata` returns the value latched pre-write (sync read); next read reflects new value; catches `debug_msg2.overwrite_cnt(31:0)` skipping a tick under read contention |
| E078 | D | planned | none | overwrite after RUN_PREPARE flush cleared occupancy: `pop_flush_cam_done=1` resets `write_pointer<=0` | next push lands on slot 0 (empty), NO overwrite; verify `debug_msg2.overwrite_cnt` reset to 0 by flush decision_reg=3; catches residual overwrite counter across run boundaries |
| E079 | D | planned | none | overwrite when side_ram RDW old-data path is valid (the intended path) | push_write then next-cycle push_state=ERASE based on `side_ram_dout_valid=1 AND addr_occupied=1 AND push_write_grant_reg=1`; all three must be high simultaneously; catches any one of three signals missing |
| E080 | D | planned | none | overwrite when `push_erase_grant` races with `pop_erase_req` on the same cycle | `decision=1` wins (push_erase priority over pop_erase in `proc_memory_arbiter_comb`); pop_erase is deferred one cycle; catches pop_erase stealing the erase slot |
| E081 | D | planned | none | overwrite counter reaches 2^32-1 then wraps | pump > 4G overwrites (conceptual — scale via instrumented counter); wrap from 0xFFFF_FFFF to 0; catches saturating arithmetic where a wrap is acceptable but fill_level goes negative |
| E082 | D | planned | none | overwrite on back-to-back wraps (4 full laps = 2048 pushes) with no pop | `debug_msg2.overwrite_cnt=1536` (= 2048-512); FILL_LEVEL stays capped at 512; catches counter growth beyond expected |
| E083 | D | planned | none | overwrite + pop_erase bang-bang: pop is draining while push is wrapping | arbiter decision alternates 0,1,2,0,1,2; push_write must always win over idle, push_erase over pop_erase, pop_erase has lock only when `pop_engine_state/=IDLE`; catches any swap in the decision priority table |
| E084 | D | planned | none | overwrite detection blocked by filter_inerr=1: errored hit that would have overwritten is dropped | wrong hit filtered; no push, no overwrite; catches inerr-filter running AFTER deassembly_fifo (would have caused overwrite) |
| E085 | D | planned | none | overwrite of slot with tsglitcherr-marked content: data already wrong | push replaces that slot; new hit drain-able cleanly; catches old bad content leaking to output through RDW |
| E086 | D | planned | none | overwrite then SEARCH finds newly-written key in same slot on the next cycle | push_write at slot X with key K1 at cycle C; overwrite (push_erase of old K0) at C+1; SEARCH for K1 at C+2 must find slot X; catches CAM internal write delay > 1 cycle |
| E087 | D | planned | none | overwrite where `pop_partition_snapshot` was already latched: overwrite must not change snapshot mid-drain | SEARCH completes at C; overwrite at slot X (partition P) at C+2; DRAIN at C+4 uses old snapshot; catches snapshot being mutated mid-drain |
| E088 | D | planned | none | overwrite crossing exactly `write_pointer` overflow at (MAIN_CAM_ADDR_WIDTH=9) MSB boundary | write_pointer = 0x1FF → 0x000; erase address = 0x1FF (from `write_pointer-1` which evaluates pre-increment); catches `write_pointer-1` wrap producing 0x1FF correctly (modulo 2^9) |
| E089 | D | planned | none | overwrite counter reset only via `decision_reg=3` (FLUSHING) per `proc_debug_counter` | writing CSR addr 8 does nothing (commented out); verify only PREP flush clears it; catches CSR write spuriously resetting counter |
| E090 | D | planned | none | overwrite during `csr.soft_reset=1` write: soft reset is stubbed (line 709 clears it on next idle) | verify soft_reset has no effect on counters currently (TODO); `debug_msg2.overwrite_cnt` unchanged; catches future soft-reset wiring that accidentally clears counters |
| E091 | D | planned | none | partition 0 only loaded: `pop_rr_idx` stays at 0 until drained, no round-robin advance on empty partitions | DRAIN: `pop_partition_pending(0)=1`, others=0; when pending(0)=1 and has_more=1 → `pop_rr_idx<=pop_issue_partition_idx` (stay); catches rr_idx skipping on has_more=1 |
| E092 | D | planned | none | round-robin advance: partition 0 has_more=0 after last hit → `pop_rr_idx<=issue_next_rr_idx_v` (=1) | verify rr_idx transitions to 1 on that cycle; catches stuck rr_idx when has_more=0 |
| E093 | D | planned | none | partition 3 → wrap to partition 0: `pop_rr_idx=3 AND pop_issue_partition_idx=3, has_more=0` → `issue_next_rr_idx_v=0` | rr_idx transitions 3→0; catches modulo wrap in `next_rr_idx_v` assignment (lines 955-959 and 960-964) |
| E094 | D | planned | none | tie-break: partition 0 and partition 2 both have pending=1, partition 1 empty | rr_idx=0 drains P0 first, then rr_idx walks 1 (empty, `next_rr_idx_v` skip in one cycle per line 1112), then 2; verify exactly one idle cycle on P1; catches two or zero idle cycles |
| E095 | D | planned | none | partition encoder result_valid gap: `i_advance` pulse without matching pipe result yet | advance during pipe in-flight → `start_eval<=1`, result_valid forced to 0; DRAIN waits in pending-wait branch (lines 1092-1097 `pop_partition_advance=1`); catches DRAIN issuing pop_erase before new LSB is valid |
| E096 | D | planned | none | encoder PIPE_STAGES=4 (default) drain rate limit | each issue requires 4-cycle pipeline: advance, leaf, mid, result; verify one hit per ~4 cycles in steady state per partition; catches pipe stages skipped producing wrong LSB |
| E097 | D | planned | none | encoder LEAF_WIDTH=16 boundary: match in leaf 0 bit 15 and leaf 1 bit 0 | N_LEAVES_CONST = 128/16 = 8; leaf 0's `leaf_lsb_addr_comb(0)=15`, leaf 1's `leaf_lsb_addr_comb(1)=16`; after clearing bit 15 via `clear_onehot_bit(..., 15)`, next result should be 16; catches stale leaf_lsb_addr_reg |
| E098 | D | planned | none | encoder MID group boundary: match in mid 0 last leaf vs mid 1 first leaf | N_MIDS_CONST = ceil(8/8)=1 for PARTITION_SIZE=128; so mid group covers all 8 leaves; verify mid path used (PIPE_STAGES=3/4) vs leaf-only path (PIPE_STAGES=1/2); catches mid_flag_reg stuck |
| E099 | D | planned | none | priority inversion attempt: P3 has single hit, P0 has 64 hits; with rr_idx starting at 0, P0 drains first | expected behavior is fairness per round-trip not strict priority; P0 drains all 64 before P3 gets a turn? NO — has_more=1 keeps rr_idx at 0 until P0 done. Verify P3 starves until P0 complete; catches an unintended priority inversion |
| E100 | D | planned | none | fairness across 4 partitions each with 1 hit | rr_idx walks 0,1,2,3 one issue each, then subheader complete via pop_last_hit_pending=1; catches any partition being skipped |
| E101 | D | planned | none | encoder stall: one partition has PIPE_STAGES=4 latency while others are PIPE_STAGES=1 ready | not mixable per instance (generics are uniform), BUT within a single drain, `all_ready_v` in COUNT waits for every `pop_partition_result_valid(i)=1` per line 1021-1028; verify COUNT stalls appropriately; catches COUNT exiting before slowest encoder reports |
| E102 | D | planned | none | arbiter tie: `req(0)=1 AND req(2)=1` (push_write AND pop_erase) with `pop_engine_state/=IDLE` | decision=2 (pop wins); catches push_write leaking through the `elsif (pop_engine_state /= IDLE)` branch logic order |
| E103 | D | planned | none | arbiter tie: `req(1)=1 AND req(3)=1` (push_erase AND flush) | decision=3 (flush highest); catches push_erase preempting flush |
| E104 | D | planned | none | arbiter tie: `req(0)=1 AND req(1)=1` (push_write AND push_erase) | decision=1 (erase wins per line 1231); catches push_write taking erase slot (would overrun ERASE state) |
| E105 | D | planned | none | arbiter lockout: `pop_engine_state/=IDLE` but `req(2)=0 AND req(0)=0` | decision=4 (idle); `push_write_grant=0`; catches decision staying at last value when no request |
| E106 | D | planned | none | arbiter on IDLE entry: `pop_engine_state=IDLE` and `req(0)=1` only | decision=0 (push_write allowed); catches push_write blocked by IDLE misread |
| E107 | D | planned | none | encoder advance pulse during load in same cycle: `i_load=1 AND i_advance=1` | `start_eval<=i_load|...`; `eval_match_vec<=i_cam_address_onehot` (load wins per line 141); catches advance clearing one bit of incoming snapshot |
| E108 | D | planned | none | encoder load when `result_valid_reg=0 AND i_advance=1` | second disjunct of start_eval is 0 (needs result_valid AND flag AND has_more); `start_eval=i_load=1`; catches advance-only without valid producing spurious eval |
| E109 | D | planned | none | encoder has_more falling edge: advance on last match clears `active_match_vec` to zero | next start_eval fails match; `result_flag_reg<=0`; drain exit; catches has_more stuck=1 after last bit cleared |
| E110 | D | planned | none | DRAIN issue_next_rr_idx_v wrap: `pop_issue_partition_idx=N_PARTITIONS-1=3` → 0 | on `has_more=0`, rr_idx<=0; catches modulo-4 wrap |
| E111 | D | planned | none | EXPECTED_LATENCY=1 (minimum allowed, below which read_time_ptr_comb branches) | write addr=3, data=1; `expected_latency_48b=0x0000_0000_0001`; first descriptor at gts=1; catches off-by-one in ≥ comparison |
| E112 | D | planned | none | EXPECTED_LATENCY=0 | at gts=0, condition `gts_8n >= 0` true immediately → `read_time_ptr_comb<=gts_8n - 0 = gts_8n`; first descriptor at gts=0 (ts[11:4]=0); catches the else branch loading the "sentinel" 0x1 |
| E113 | D | planned | none | EXPECTED_LATENCY=0xFFFF (max 16-bit) | `expected_latency_48b=0x0000_0000_FFFF`; first descriptor at gts=65535; catches latency field truncation at 16 bits vs 48-bit compare |
| E114 | D | planned | none | EXPECTED_LATENCY written to 0x0_0001 while descriptor loop is active | latency change takes effect next cycle because `expected_latency_48b` is derived in comb from `csr.expected_latency`; descriptor cadence jumps; catches stale 48-bit version |
| E115 | D | planned | none | EXPECTED_LATENCY change mid-SEARCH: new latency 5 while pop engine is in SEARCH | SEARCH uses latched `pop_current_sk`, not expected_latency; SEARCH unaffected; descriptor gen next beat uses new latency; catches descriptor gen using pre-change latency |
| E116 | D | planned | none | EXPECTED_LATENCY written with no push traffic | descriptor generator keeps firing (generator is independent of push); zero-hit subheaders at new cadence; catches generator incorrectly gated on push activity |
| E117 | D | planned | none | EXPECTED_LATENCY=0 combined with interleaving check: first descriptor fires only when `read_time_ptr(TCC8N_INTERLEAVING_HI:4)=INTERLEAVING_INDEX` | for INTERLEAVING_INDEX=0, first descriptor at gts=0; for INDEX=3 (factor 4), first descriptor at gts=48 (=0x30, ts[11:4]=3); catches interleaving offset |
| E118 | D | planned | none | EXPECTED_LATENCY vs terminate race: `run_state_cmd→TERMINATING` with gts_end_of_run latched, then latency bumped down | descriptor generator still gated by `terminating_drain_done` check, not by latency; bumped latency has no effect after EOR; catches latency change pushing descriptor past EOR |
| E119 | D | planned | none | pop_search_wait_cnt fixed 5-cycle guard (line 993) interaction with latency | SEARCH state: 4 extra cycles + comparison; total SEARCH latency is fixed regardless of EXPECTED_LATENCY; catches SEARCH wait becoming latency-dependent |
| E120 | D | planned | none | drain engine's internal delay: LOAD=1 cycle, COUNT=MATCH_COUNT_CHUNKS_CONST*N_PARTITIONS+1 cycles | PARTITION_SIZE=128, CHUNK_WIDTH=16 → CHUNKS=8; COUNT=8*4+1=33 cycles min; verify pop_count_done timing; catches COUNT duration off by chunk/partition count |
| E121 | D | planned | none | EXPECTED_LATENCY=2000 (default) with depth-bounded push cadence | steady state: push_cnt and pop_cnt advance together with delta = 2000/16 ≈ 125 descriptors in flight; catches pop falling behind push indefinitely |
| E122 | D | planned | none | scheduling observable: descriptor writes to pop_cmd_fifo at 16-cycle cadence vs drain of 1 every ~32 cycles for N=1-hit subheaders | fifo_usedw grows; at 16 pending commands (fifo full), generator stalls; catches fifo_usedw not saturating |
| E123 | D | planned | none | latency change while `pop_engine_state=DRAIN`: new latency takes effect only on next descriptor gen, not current drain | DRAIN uses `pop_current_sk` latched at IDLE exit; no mid-drain key change; catches drain using new latency to derive new descriptor |
| E124 | D | planned | none | EXPECTED_LATENCY=2 (below POP_CMD_FIFO depth × 16 = 256 gts window) | descriptor fifo can fill before drain starts; verify `pop_cmd_fifo_full=1` asserts and generator back-pressures; catches fifo overflow on low-latency mode |
| E125 | D | planned | none | scheduling: drain of N=0 subheader completes in `pop_search_wait_cnt=5 + LOAD=1 + COUNT chunks + RESET=1` cycles | minimum zero-hit subheader turn-around; catches excessive cycles for zero-hit case |
| E126 | D | planned | none | scheduling: drain of N=1 subheader with `pop_last_hit_pending` path | DRAIN enter, pop_erase grant, hits_count 1→0, RESET same cycle as last pop_erase; catches extra RESET cycle |
| E127 | D | planned | none | scheduling: drain of N=128 (full partition) subheader | 128 × (issue + erase + encoder advance) cycles; verify fixed cycle budget per hit; catches drain rate slowing due to pipeline stall |
| E128 | D | planned | none | latency vs flush interaction: PREP entered mid-flight; `pop_flush_cam_done` resets counters; next RUNNING starts descriptor gen at gts=0+latency | verify `run_mgmt_flushed=1` gating the `asi_ctrl_ready` handshake; descriptor gen restarts fresh; catches stale gts or latency carried across PREP |
| E129 | D | planned | none | latency=65535 with terminating_drain_done: EOR arrives before first descriptor | `terminating_drain_done=1` as soon as deassembly_fifo_empty AND pop_cmd_fifo_empty AND pop_engine_state=IDLE AND push==pop+overwrite; `asi_ctrl_ready=1` in TERMINATING; catches drain_done waiting for descriptor that can never fire |
