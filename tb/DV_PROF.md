# DV_PROF.md — ring_buffer_cam

**Companion to:** [DV_PLAN.md](DV_PLAN.md), [DV_HARNESS.md](DV_HARNESS.md), [FORMAL_PLAN.md](FORMAL_PLAN.md)
**Canonical ID Range:** `P001-P137`
**Intent:** random, profile, soak, and throughput-oriented scenarios for sustained-signoff confidence.

| case_id | method | implementation | legacy alias | scenario | primary checks |
|---|---|---|---|---|---|
| P001 | R | live UVM | `test_random_push_pop` | randomized same-key traffic with variable hit count and spacing | no undrained references, meaningful output coverage |
| P002 | R | live UVM | `test_random_multi_key` | randomized multi-key traffic over several key groups | all keys eventually drain and no starvation |
| P003 | R | live UVM | `test_random_throughput` | randomized deep same-key throughput run | sustained drain with no silent loss |
| P004 | D | live UVM | old `test_overwrite_stress` intent | long overwrite-pressure soak in legal config | continuous legal overwrite with explicit loss accounting |
| P005 | R | live UVM | derived from `test_overwrite_stress` intent | mixed same-key hotspot plus single-hit inserts across repeated random-length terminate/restart windows | each window drains cleanly and the per-window service span stays above a conservative throughput floor |
| P006 | R | live UVM | historical cfg sweep intent | configuration-aware partitioned encoder stress scaled to the active partition / pipe-stage build | all active partitions are exercised and the observed service span stays above a conservative configuration-aware throughput floor |

# Canonical Planned Cases (P007-P129)

| case_id | method | implementation | legacy alias | scenario | primary checks |
|---|---|---|---|---|---|
| P007 | R | live UVM | none | single-key pool=1, fixed ingress gap 31 cycles, sink-ready=100%, `EXPECTED_LATENCY=128`, 4096 hits; calibrated low-fill profile | scoreboard drains cleanly; `PUSH_COUNT==POP_COUNT`, `OVERWRITE_COUNT==0`, `CACHE_MISS_COUNT==0`; low-fill envelope stays bounded and isolated UCDB/log evidence is published |
| P008 | R | live UVM | none | single-key pool=1, fixed ingress gap 15 cycles, sink-ready=100%, `EXPECTED_LATENCY=128`, 4096 hits; calibrated medium-fill profile | scoreboard drains cleanly; no overwrite or cache-miss; medium-fill envelope stays below depth with published UCDB/log evidence |
| P009 | R | live UVM | none | single-key pool=1, fixed ingress gap 13 cycles, sink-ready=100%, `EXPECTED_LATENCY=128`, 4096 hits; calibrated high-fill no-overwrite profile | no legal-pressure loss is entered; scoreboard drains cleanly and the high-fill single-key window still closes with `OVERWRITE_COUNT==0` |
| P010 | R | live UVM | none | two-key round-robin (`pool=2`), fixed ingress gap 11 cycles, sink-ready=100%, `EXPECTED_LATENCY=128`, 4096 hits | per-key drain counts close exactly under deterministic alternation; no cross-key swap, overwrite, or cache-miss; scoreboard drains both keys with `PUSH_COUNT==POP_COUNT` |
| P011 | R | live UVM | none | four-key round-robin (`pool=4`), fixed ingress gap 13 cycles, sink-ready=100%, `EXPECTED_LATENCY=128`, 4096 hits | per-key drain integrity across four concurrent keys; no cache-miss and no overwrite in the calibrated multi-key window |
| P012 | R | live UVM | none | eight-key round-robin (`pool=8`), fixed ingress gap 13 cycles, sink-ready=100%, `EXPECTED_LATENCY=128`, 4096 hits | per-key drain integrity across eight concurrent keys; no cache-miss and no overwrite in the calibrated window |
| P013 | R | live UVM | none | sixteen-key round-robin (`pool=16`), fixed ingress gap 13 cycles, sink-ready=100%, `EXPECTED_LATENCY=128`, 4096 hits | per-key drain integrity across sixteen concurrent keys; subheader/data framing stays clean with no overwrite or cache-miss |
| P014 | R | live UVM | none | deterministic full lane-local key sweep (`pool=64`), fixed ingress gap 15 cycles, sink-ready=100%, `EXPECTED_LATENCY=128`, 4096 hits | every swept key drains cleanly; `CACHE_MISS_COUNT==0`; the full lane-local keyspace is exercised without the unstable random-full-keyspace artifact |
| P015 | R | live UVM | none | four-key burst-gap profile: 16-hit bursts, 176 idle cycles, sink-ready=100%, `EXPECTED_LATENCY=128`, 4096 hits | burst/idle boundaries do not lose hits; scoreboard drains cleanly and burst-driven fill growth stays bounded with `OVERWRITE_COUNT==0` |
| P016 | R | live UVM | none | four-key Bernoulli-arrival low-rate profile, threshold 24, sink-ready=100%, `EXPECTED_LATENCY=128`, 4096 hits | low-rate stochastic fill distribution is exercised; scoreboard drains cleanly with `OVERWRITE_COUNT==0` and published UCDB/log evidence |
| P017 | R | live UVM | none | four-key Bernoulli-arrival higher-rate profile, threshold 28, sink-ready=100%, `EXPECTED_LATENCY=128`, 4096 hits | pushes run close to the legal no-overwrite boundary while still draining cleanly; `OVERWRITE_COUNT==0` and no cache-miss in the calibrated high-rate stochastic regime |
| P018 | R | live UVM | none | four-key profile with fixed ingress gap 15 cycles, deterministic 75%-ready sink gating, `EXPECTED_LATENCY=128`, 4096 hits | sink backpressure is exercised live; scoreboard drains cleanly, `OVERWRITE_COUNT==0`, and the case proves the harness can force `ready=0` during active traffic |
| P019 | R | live UVM | none | four-key profile with fixed ingress gap 23 cycles, deterministic 50%-ready sink gating, `EXPECTED_LATENCY=128`, 4096 hits | heavier sink backpressure is exercised live; scoreboard drains cleanly with no overwrite and bounded fill under the calibrated 50%-ready envelope |
| P020 | R | live UVM | none | four-key profile with fixed ingress gap 17 cycles, PRNG-driven ~70%-ready sink gating, `EXPECTED_LATENCY=128`, 4096 hits | randomized backpressure transitions are exercised live; scoreboard drains cleanly with `OVERWRITE_COUNT==0` and real ready-high/low evidence |
| P021 | R | live UVM | none | four-key profile with fixed ingress gap 15 cycles, sink-ready=100%, `EXPECTED_LATENCY` chosen at run start from `{1,128,2000}`, 4096 hits | selected latency readback matches the programmed value; the reference model honors the chosen latency and the run drains cleanly without stuck hits |
| P022 | R | live UVM | none | four-key profile with fixed ingress gap 15 cycles, sink-ready=100%, `EXPECTED_LATENCY=1`, 4096 hits | min-latency service opens early but still drains cleanly; no overwrite or cache-miss in the calibrated min-latency profile |
| P023 | R | live UVM | none | four-key profile with fixed ingress gap 15 cycles, sink-ready=100%, `EXPECTED_LATENCY=2000`, 4096 hits | long-latency service window is held open long enough to build backlog, then drains cleanly with `OVERWRITE_COUNT==0` |
| P024 | R | live UVM | none | exact-depth single-key boundary: fill to `N_ENTRY=512` at `EXPECTED_LATENCY=2000`, then inject one extra hit | exact-depth precondition is reached cleanly; the depth+1 push produces exactly one overwrite and the final accounting closes as `PUSH=513`, `POP=512`, `OVERWRITE=1` |
| P025 | R | live UVM | none | pool=1, calibrated near-fixed-point uniform push, fixed ingress gap 16 cycles, 100% ready, `EXPECTED_LATENCY=128`, 50k hits | no overwrite or cache-miss across the long steady-state run; final drain closes cleanly; published max-fill/max-residency evidence stays in the calibrated bounded band |
| P026 | R | live UVM | none | pool=4 concurrent keys, equal-weight `1:1:1:1`, fixed ingress gap 13 cycles, `EXPECTED_LATENCY=128`, 4096 hits | per-key drain counts close exactly at `1024` each; no overwrite, no cache-miss, and the calibrated equal-weight profile stays lossless |
| P027 | R | live UVM | none | pool=4 concurrent, skewed weight `7:2:1:1`, fixed ingress gap 13 cycles, `EXPECTED_LATENCY=128`, 2816 hits | weighted drain counts close exactly at `1792/512/256/256`; no overwrite, no cache-miss, and no skew amplification at drain |
| P028 | R | live UVM | none | pool=8 concurrent, calibrated Zipfian weight (s=1.2), fixed ingress gap 13 cycles, `EXPECTED_LATENCY=128`, 40k hits | per-key drain counts close exactly at `17145/7463/4588/3248/2485/1997/1660/1414`; every key emits data-bearing subheaders; no overwrite or cache-miss in the long skewed fairness profile |
| P029 | R | live UVM | none | pool=4 concurrent, deterministic round-robin interleave (`k0,k1,k2,k3,...`), fixed ingress gap 13 cycles, `EXPECTED_LATENCY=128`, 20k hits | each key drains exactly `5000` hits under deterministic interleave; all four keys emit data-bearing subheaders with no overwrite or cache-miss |
| P030 | R | live UVM | none | pool=4 concurrent, calibrated 256-hit key bursts, fixed ingress gap 13 cycles, 176-cycle epoch gap, `EXPECTED_LATENCY=128`, 2048 hits | 256-hit burst packetization closes cleanly at `512` hits per key with `OVERWRITE_COUNT==0`; corrected from the earlier overdriven zero-gap profile |
| P031 | R | live UVM | none | two-key adversarial alternation (`k0,k1,k0,k1,...`), fixed ingress gap 11 cycles, 100% ready, `EXPECTED_LATENCY=128`, 20k hits | overlap monitor sees ingress during SEARCH/LOAD/COUNT and drain while ingress is still active; both keys drain exactly `10k` hits with no overwrite or cache-miss |
| P032 | R | live UVM | none | pool=8 concurrent, randomized interleave with key-pool reuse rate 50%, fixed ingress gap 13 cycles, `EXPECTED_LATENCY=128`, 4096 hits | randomized eight-key reuse drains cleanly; all eight keys are active; `CACHE_MISS_COUNT==0` and epoch isolation holds end-to-end |
| P033 | R | live UVM | none | pool=16 concurrent, randomized interleave with key-pool reuse rate 80%, fixed ingress gap 13 cycles, `EXPECTED_LATENCY=128`, 4096 hits | randomized sixteen-key reuse drains cleanly across the full pool; `CACHE_MISS_COUNT==0` and no unmatched search escapes the scoreboard |
| P034 | R | live UVM | none | three active keys plus one silent searched key, fixed ingress gap 13 cycles, `EXPECTED_LATENCY=128`, 3072 hits | silent-key searches emit zero-hit SOP+EOP subheaders while `CACHE_MISS_COUNT` stays `0`; active-key drain accounting remains lossless |
| P035 | R | live UVM | none | interleaved five-key window: three active keys (`2/4/6`) with two silent searched keys (`3/5`), fixed ingress gap 13 cycles, `EXPECTED_LATENCY=128`, 3072 hits | both silent keys emit zero-hit subheaders with `CACHE_MISS_COUNT==0`; active-key drain accounting remains lossless across the interleaved window |
| P036 | R | live UVM | none | pool=4 concurrent, randomized push order with fixed seed `0x03601A2B`, fixed ingress gap 13 cycles, `EXPECTED_LATENCY=128`, 30k hits | distinct seeded evidence run A; scoreboard drains cleanly, all four keys stay active, and standalone UCDB/log artifacts are published |
| P037 | R | live UVM | none | pool=4 concurrent, randomized push order with fixed seed `0x03703C4D`, fixed ingress gap 13 cycles, `EXPECTED_LATENCY=128`, 30k hits | distinct seeded evidence run B; scoreboard drains cleanly, all four keys stay active, and the seed axis is explicit in the case metadata |
| P038 | R | live UVM | none | pool=4 concurrent, randomized push order with fixed seed `0x03805E6F`, fixed ingress gap 13 cycles, `EXPECTED_LATENCY=128`, 30k hits | distinct seeded evidence run C; scoreboard drains cleanly, all four keys stay active, and the third orthogonal seed grows the isolated evidence set monotonically |
| P039 | R | live UVM | none | pool=4 concurrent, weighted fairness focus `2:1:1:1`, fixed ingress gap 13 cycles, `EXPECTED_LATENCY=128`, 5120 hits | heavy/light drain counts close exactly at `2048/1024/1024/1024` with `OVERWRITE_COUNT==0`; weighted fairness evidence is now published from a calibrated lossless run |
| P040 | R | live UVM | none | pool=4 concurrent, seeded random cross-key integrity audit (`seed=0x0400F10F`), fixed ingress gap 13 cycles, `EXPECTED_LATENCY=128`, 20k hits | seeded integrity anchor closes with all four keys active, no overwrite, no cache-miss, and published standalone UCDB/log evidence |
| P041 | R | live UVM | none | pool=4 concurrent + random backpressure p=0.7, 30k txn | multi-key backpressure cross extends the live ready-gating already proven by `P018-P020`; scoreboard drains EOT cleanly with no unexplained loss |
| P042 | R | live UVM | none | pool=8 concurrent with random late arrivals (50% arrive after others already occupy CAM), 100% ready, 30k txn | late-started keys become active without displacing earlier keys; per-key accounting closes and no cache-miss or unexplained drain loss appears |
| P043 | R | live UVM | none | pool=8 concurrent + EXPECTED_LATENCY swept mid-run, 30k txn | latency change while multi-key active does not corrupt scoreboard; `EXPECTED_LATENCY` readback matches last write |
| P044 | R | live UVM | none | pool=4 concurrent, key-pool reuse at 100% (same 4 keys cycled continuously), 40k txn | long same-key sequence coverage; per-key retire-latency distribution steady across run |
| P045 | R | live UVM | none | pool=16 concurrent, uniform random, 100% ready, 50k txn | stress on arbitration across many keys; scoreboard per-key occupancy histogram bounded by depth/pool |
| P046 | D | live UVM | none | active-build partition-local profile: in `default_p2_pipe4`, fill only partition 0 of one same-key epoch and prove all issues stay local to partition 0 | single-partition issue mask stays `p0` only, the target key drains exactly the programmed hit count, and no cross-talk reaches the other live partition |
| P047 | D | live UVM | none | active-build partition handoff profile after exact-partition prefill: in `default_p2_pipe4`, prefill/drain one full partition, then drive a target epoch that exposes the next-partition handoff window | the target epoch issues across both live partitions (`p0->p1` handoff) after the exact-partition prefill; accounting closes exactly with no overwrite or cache-miss |
| P048 | D | live UVM (`p4_n4_pipe4` variant) | none | partition-local profile on partition 2 in the `p4` build variant | same contract as `P046/P047`, but on partition 2 of the 4-partition variant, with all focus-key issues staying in partition 2 |
| P049 | D | live UVM (`p4_n4_pipe4` variant) | none | partition-local profile on partition 3 in the `p4` build variant | same contract as `P046/P047`, but on partition 3 of the 4-partition variant, with all focus-key issues staying in partition 3 |
| P050 | R | live UVM | none | active-build partition-balance profile: balanced traffic across every live partition in the selected build (50/50 on `default_p2_pipe4`, 25/25/25/25 on `p4`) | per-partition drain throughput stays within the calibrated fairness band across the active build partitions |
| P051 | R | live UVM | none | active-build partition-skew profile: heavy/light partition weighting in the selected build (90/10 on `default_p2_pipe4`, 70/10/10/10 on `p4`) | the dominant partition stays hot until exhausted, the minority partitions retire afterward with no accounting loss, and no residual fill remains |
| P052 | R | live UVM | none | active-build partition-contention profile: near-even heavy pair on the selected build (50/50 on `default_p2_pipe4`, 40/40/10/10 on `p4`) | the active heavy partitions contend fairly and the measured per-partition service does not collapse under sustained contention |
| P053 | R | live UVM | none | active-build adversarial partition skew (99/1 on `default_p2_pipe4`, 99/1/0/0 on `p4`) | the heaviest partition dominates legally, the minority partition retires only after the dominant partition exhausts, and the final accounting still closes exactly |
| P054 | R | live UVM (`p2_pipe1` variant) | none | `ENCODER_PIPE_STAGES=1` build-variant profile with balanced active-build partition traffic | pipe-stage=1 encoder contract is covered, balanced traffic drains across both active partitions, and the single-hit smoke sees the expected 2-cycle visible load-to-result latency |
| P055 | R | live UVM (`p2_pipe2` variant) | none | `ENCODER_PIPE_STAGES=2` build-variant profile with balanced active-build partition traffic | pipe-stage=2 encoder contract is covered, balanced traffic drains across both active partitions, and the single-hit smoke sees the expected 2-cycle visible load-to-result latency |
| P056 | R | live UVM (`p2_pipe3` variant) | none | `ENCODER_PIPE_STAGES=3` build-variant profile with balanced active-build partition traffic | pipe-stage=3 encoder contract is covered, balanced traffic drains across both active partitions, and the single-hit smoke sees the expected 3-cycle visible load-to-result latency |
| P057 | R | live UVM (`default_p2_pipe4` variant) | none | `ENCODER_PIPE_STAGES=4` build-variant profile with balanced active-build partition traffic | pipe-stage=4 encoder contract is covered, balanced traffic drains across both active partitions, and the single-hit smoke sees the expected 4-cycle visible load-to-result latency |
| P058 | R | live UVM | none | active-build partition handoff profile: prefill partition 0 to 80%, then drive only the next live partition | the prefetched partition drains to empty while the next partition fills, with no stall on the empty-partition state |
| P059 | R | live UVM | none | active-build partition saturation and recovery: fill one live partition to 100%, stall sink-ready for 500 cyc, then release | overwrite accounting is correct during the stall window and the residual drains without silent loss once backpressure is released |
| P060 | R | live UVM | none | active-build encoder-stall profile under sink-ready=30% with balanced partition traffic | encoder load/advance handshake does not deadlock and `FILL_LEVEL` oscillates within the calibrated band |
| P061 | R | live UVM | none | active-build cross-partition round-robin push across every live partition in the selected build | per-partition epoch counts stay balanced and the overlap monitor sees the intended cross-partition concurrency window |
| P062 | R | live UVM | none | active-build cross-partition burst profile: burst one live partition, then the next, across the selected build | partition-switch transients are visible and each burst drains intact before the following window is audited |
| P063 | R | live UVM | none | sparse active-partition alternation across the selected build (for `default_p2_pipe4`: alternate the lowest and highest live partitions only) | idle-partition coverage is hit while the active partitions still preserve fairness and drain integrity |
| P064 | R | live UVM | none | active-build partition profile with time-varying sink-ready while balanced traffic spans every live partition | each live partition drain path is exercised under backpressure without deadlock or silent accounting drift |
| P065 | R | live UVM (variant-only) | none | build-axis comparison run: selected partition/traffic profile replayed on `addr_enc_logic_partitioned` and `addr_enc_logic_pipe` variants | both encoder variants produce equivalent scoreboard results and the commit/drain timing differs only by the documented pipeline offset |
| P066 | R | live UVM | none | pool=4, uniform push, TERM injected once at pseudo-random time in [1k, 9k] cyc, then lane-local end-of-run is sent while traffic is still offered | after `endofrun_seen`, ingress backpressures immediately; accepted hits stop advancing and all pre-EOR work drains or is explicitly rejected at the source |
| P067 | R | live UVM | none | pool=4, uniform push, FLUSH injected once at random time, run length 10k | post-FLUSH `FILL_LEVEL==0`; scoreboard drops pending refs with flush disposition; counter drift bounded |
| P068 | R | live UVM | none | pool=4, uniform push, TERM then RESTART within same run, 20k txn | counters reset semantics on restart validated (per B029 contract); post-restart scoreboard starts clean |
| P069 | R | live UVM | none | pool=4, uniform push, FLUSH rate = 1 per 5k cyc, run length 30k cyc | multiple flush windows; `POP_COUNT` + flushed-count == `PUSH_COUNT`; no leak across flush windows |
| P070 | R | live UVM | none | pool=8 multi-key, TERM injected at 70% fill, 20k txn | terminate at high-fill exercises stale-state clear; no key drains after TERM |
| P071 | R | live UVM | none | pool=8 multi-key, FLUSH injected at 90% fill, 20k txn | flush at near-full fill; overwrite pressure does not confuse flush path |
| P072 | R | live UVM | none | pool=4, TERM rate = 1 per 2k cyc (high injection), 30k cyc | sustained high-rate TERM/RESTART; scoreboard state machine tolerates rapid run-control cycles |
| P073 | R | live UVM | none | pool=4, FLUSH rate = 1 per 2k cyc, 30k cyc | sustained high-rate FLUSH; counter drift bounded per FLUSH window |
| P074 | R | live UVM | none | pool=4, TERM/FLUSH randomly interleaved at random times, 30k cyc | mixed run-control injection; each event classified by state observer; scoreboard reconciles residuals per event type |
| P075 | R | live UVM | none | pool=4, TERM injected mid-subheader emit (egress valid high), 20k cyc | mid-frame TERM handled legally; out_monitor confirms subheader framing not split; aborted drain flag set in scoreboard |
| P076 | R | live UVM | none | pool=4, FLUSH injected mid-subheader emit, 20k cyc | mid-frame FLUSH aborts drain cleanly; no stale EOP without SOP |
| P077 | R | live UVM | none | pool=4, TERM with ~32 in-flight hits, 10k cyc | specific in-flight count axis; all 32 accounted for as drained-or-aborted; `PUSH_COUNT - POP_COUNT` matches aborted count |
| P078 | R | live UVM | none | pool=4, TERM with ~256 in-flight hits, 10k cyc | larger in-flight population; same accounting rule; fill trajectory around TERM captured |
| P079 | R | live UVM | none | pool=4, FLUSH with ~32 in-flight hits, 10k cyc | flush aborts 32 refs; `OVERWRITE_COUNT` unchanged (flush is not overwrite); flushed count matches scoreboard |
| P080 | R | live UVM | none | pool=4, FLUSH with ~256 in-flight hits, 10k cyc | flush aborts 256 refs with same rule |
| P081 | R | live UVM | none | pool=8 multi-key, TERM/FLUSH at partition boundary (during cross-partition drain), 20k cyc | partition-aware abort handling; per-partition residual counted separately |
| P082 | R | live UVM | none | pool=4, TERM when `CACHE_MISS_COUNT` was just incremented, 10k cyc | cache-miss state cleared cleanly by TERM; post-RESTART `CACHE_MISS_COUNT` resets per B029 |
| P083 | R | live UVM | none | pool=4, TERM injected during sink-ready=0 stall window, 10k cyc | TERM during backpressure does not hang; state observer timestamps TERM acceptance within bounded cycles |
| P084 | R | live UVM | none | pool=4, FLUSH followed immediately by heavy push burst, 20k cyc | post-flush acceptance gate works; no residual-hit leak into post-flush epoch |
| P085 | R | live UVM | none | pool=4, random TERM/FLUSH injection while `EXPECTED_LATENCY` is being reprogrammed, 20k cyc | run-control and CSR reprogram do not race; readback of `EXPECTED_LATENCY` consistent across event |
| P086 | R | live UVM | none | long soak, pool=4 uniform, seed=0x1, 1M txn, checkpoints at txn=1,2,4,8,16,32,64,128,256,512,1k,2k,4k,8k,16k,32k,64k,128k,256k,512k,1M | per checkpoint UCDB under `cov_after/txn_growth/P086/`; coverage growth curve shows per-key retire-latency bins saturate first (<64k), partition-fairness bins saturate last (>256k) |
| P087 | R | live UVM | none | long soak, pool=4 uniform, seed=0x2 (orthogonal to P086), 1M txn, same checkpoint schedule | UCDB merged with P086 gives seed-axis diversity; same-key burst bins saturate first, backpressure bins remain unsaturated (documented gap) |
| P088 | R | live UVM | none | long soak, pool=8 Zipfian s=1.2, seed=0x3, 1M txn | heavy-key bin saturates first (<16k); tail-key bins saturate last (>512k); checkpoint curve flagged as multi-key reference run |
| P089 | R | live UVM | none | long soak, pool=16 uniform, seed=0x4, 1M txn | wide-key coverage; key-value toggle coverage saturates ~256k; retire-ordering cross bins saturate last |
| P090 | R | live UVM | none | long soak, pool=4 Poisson lambda=0.5, seed=0x5, 1M txn | fill-level distribution bins saturate early; EXPECTED_LATENCY cross bins remain unsaturated (requires P091) |
| P091 | R | live UVM | none | long soak, pool=4 Poisson lambda=0.9, seed=0x6, 1M txn, EXPECTED_LATENCY randomized every 50k cyc | EXPECTED_LATENCY x fill-level cross bins saturate by 500k; `OVERWRITE_COUNT` coverage bin hit only at lambda=0.9 |
| P092 | R | live UVM | none | long soak, pool=4 uniform, seed=0x7, TERM injected every 100k cyc, 1M txn | run-control transition coverage saturates <200k; per-TERM residual accounting matches scoreboard |
| P093 | R | live UVM | none | long soak, pool=4 uniform, seed=0x8, FLUSH injected every 100k cyc, 1M txn | flush-state coverage saturates <200k; `POP_COUNT` trajectory bounded by flush schedule |
| P094 | R | live UVM | none | long soak, N_PARTITIONS=4 balanced, seed=0x9, 1M txn | partition-balance fairness coverage saturates ~64k; partition-empty cross bin saturates last |
| P095 | R | live UVM | none | long soak, N_PARTITIONS=4 skewed (70/10/10/10), seed=0xA, 1M txn | partition-skew coverage saturates ~128k; minority-partition retire-latency bin saturates >512k |
| P096 | R | live UVM | none | long soak, pool=4, seed=0xB, `INERR_COUNT` intentionally driven via asi_hit_type1_error pulses at 1%, 1M txn | error-hit acceptance coverage; `INERR_COUNT` monotone; scoreboard does not count errored hits as references |
| P097 | R | live UVM | none | long soak, pool=4, seed=0xC, random sink-ready with p=0.7, 1M txn | backpressure coverage bins advance through checkpoint curve; documented gap closes once tb_top wiring lands |
| P098 | R | live UVM | none | long soak, pool=4, seed=0xD, random sink-ready with p=0.5, 1M txn | deeper backpressure axis; `FILL_LEVEL` high-bin saturates first |
| P099 | R | live UVM | none | long soak, pool=8, seed=0xE, ENCODER_PIPE_STAGES=2, 1M txn | encoder-variant axis for merge; saturation curve distinct from P100 (stages=4) |
| P100 | R | live UVM | none | long soak, pool=8, seed=0xF, ENCODER_PIPE_STAGES=4, 1M txn | encoder-variant axis complement to P099; merged UCDB covers pipe-stage cross |
| P101 | R | live UVM | none | long soak, pool=4 with reuse rate 100%, seed=0x10, 2M txn | long-reuse coverage; same-key reentry bin saturates >1M only; slowest-saturating bin in the suite |
| P102 | R | live UVM | none | long soak, pool=full keyspace (2^8), uniform key draw, seed=0x11, 2M txn | keyspace toggle coverage saturates ~256k; cross with partition-fairness at 1M |
| P103 | R | live UVM | none | long soak, pool=4, seed=0x12, mid-run `EXPECTED_LATENCY` sweep min->max every 200k cyc, 2M txn | latency-axis coverage exhaustive; scoreboard ref-model updates latency expectation per sweep |
| P104 | R | live UVM | none | long soak, pool=4, seed=0x13, TERM + FLUSH interleaved every 50k cyc, 2M txn | combined run-control coverage; state-observer logs every transition; per-event residual totals reconciled |
| P105 | R | live UVM | none | long soak, pool=4, seed=0x14, adversarial arbiter interleave (per P031 shape), 2M txn | overlap monitor bins saturate ~128k; arbiter-winner cross with partition-id axis |
| P106 | R | live UVM | none | long soak, pool=4, seed=0x15, silent-key search rate 10%, 2M txn | zero-hit silent-key subheader coverage saturates early while `CACHE_MISS_COUNT` stays flat; active-key drain unaffected; silent-key cross bin hit |
| P107 | R | live UVM | none | long soak, pool=4, seed=0x16, burst length random in [1,256], 2M txn | same-key burst-length distribution coverage fully saturated; max-burst bin tracked |
| P108 | R | live UVM | none | long soak, pool=4, seed=0x17, ingress gap random in [0,7], 2M txn | ingress-gap distribution coverage; commit-rate variance bounded |
| P109 | R | live UVM | none | long soak, pool=4, seed=0x18, side_data forced to sweep full 31-bit range monotonically, 2M txn | side_data toggle coverage; scoreboard per-ref side_data match rate 100% |
| P110 | R | live UVM | none | long soak merge-reference: combined run using seeds 0x1..0x18, 2M txn replay per seed, merged UCDB | merged coverage from P086-P109 rolls into one `cov_after/txn_growth/P110/merged.ucdb`; closure report shows which bins still below 100% |
| P111 | R | live UVM | none | overwrite-pressure soak: one same-ts hotspot (pool=1), mildly bursty so the pre-service arrival window exceeds 512 by a small margin before first pop, 50k txn equivalent | pushes-before-first-pop minus 512 defines the lower bound on legal overwrites; `FILL_LEVEL` reaches depth; no unexplained loss beyond overwrite accounting |
| P112 | R | live UVM | none | overwrite-pressure soak: one same-ts hotspot (pool=1), stronger burst so the pre-service arrival window exceeds 512 by a medium margin, 50k txn equivalent | overwrite lower bound scales with burst excess above 512; depth saturation and `PUSH = POP + OVERWRITE + residual` invariant hold |
| P113 | R | live UVM | none | overwrite-pressure soak: one same-ts hotspot (pool=1), aggressive burst with sustained over-injection before service catches up, 50k txn equivalent | hotspot reaches full sector depth quickly; overwrite begins before first pop service; no undrained residual beyond the explicit overwrite model |
| P114 | R | live UVM | none | overwrite-pressure soak: pool=1, push=drain rate but sink-ready=50%, 30% expected overwrite, 50k txn | overwrite arises from sink backpressure rather than push excess; sink throttling lowers effective service and the accounting invariant still holds |
| P115 | R | live UVM | none | overwrite-pressure soak: pool=1, push=drain rate but sink-ready variable p=0.3, 50% expected overwrite, 50k txn | stochastic sink backpressure drives a heavier overwrite regime than P114 while the accounting invariant still holds |
| P116 | R | live UVM | none | overwrite-pressure soak: pool=4 balanced, push rate 120%, 20% overwrite per key, 50k txn | per-key overwrite fairness: each key overwrites at equal rate within ±5% |
| P117 | R | live UVM | none | overwrite-pressure soak: pool=4 skewed (70/10/10/10), push rate 120%, 50k txn | overwrite concentrates on heaviest key; minority keys see near-zero overwrite |
| P118 | R | live UVM | none | overwrite-pressure soak: pool=4 adversarial (alternating dominant key across restart windows), push rate 130%, 50k txn | overwrite workload rotates across keys; per-window overwrite ownership follows the dominant key |
| P119 | R | live UVM | none | overwrite-pressure soak: one same-ts hotspot with bursty pattern (push 2x rate for 256 cyc then 0.5x for 256 cyc), 50k txn | overwrite occurs only in the burst window; `FILL_LEVEL` returns toward low-water in the recovery window; burstiness, not just average rate, is visible in the counter trace |
| P120 | R | live UVM | none | overwrite-pressure soak: pool=1, slow creep (push rate 101%), 200k txn | low-rate overwrite accumulation; `OVERWRITE_COUNT` stays in a low-percent envelope while the hotspot still saturates; long-running invariant holds |
| P121 | R | live UVM | none | overwrite-pressure soak: pool=4, push rate 130%, FLUSH every 20k cyc, 100k txn | flush clears fill between overwrite windows; `OVERWRITE_COUNT` bounded per window; post-flush fill=0 |
| P122 | R | live UVM | none | overwrite-pressure soak: pool=4, push rate 130%, TERM every 20k cyc with RESTART, 100k txn | terminate under overwrite pressure; each restart replays the same overwrite window cleanly and drains to zero residual fill |
| P123 | R | live UVM | none | overwrite-pressure soak: one same-ts hotspot, `EXPECTED_LATENCY=max` so hits linger and the 512-entry service window stays closed longer, 50k txn | larger pushes-before-first-pop window than default latency; overwrite rate increases for the same burst shape |
| P124 | R | live UVM | none | overwrite-pressure soak: one same-ts hotspot, `EXPECTED_LATENCY=min` so the service window opens early, 50k txn | same burst shape produces a smaller pushes-before-first-pop count than P123; overwrite pressure drops accordingly |
| P125 | R | live UVM | none | overwrite-pressure checkpoint soak: balanced four-key profile with log-spaced UCDB capture and optional `DV_LONG_TXN_OVERRIDE` for reduced nightly reruns | overwrite-axis coverage growth curve is published and balanced per-key overwrite ownership stays inside the calibrated band |
| P126 | R | live UVM | none | overwrite-pressure checkpoint soak: four-key Zipf-like profile with log-spaced UCDB capture and optional `DV_LONG_TXN_OVERRIDE` for reduced nightly reruns | skewed overwrite distribution stays ordered by key hotness while the checkpoint curve and final overwrite accounting still close exactly |
| P127 | R | live UVM | none | overwrite-pressure checkpoint soak: four-key near-full prefill followed by ~30%-ready sink backpressure and log-spaced UCDB capture | backpressure-driven overwrite axis stays distinct from push-excess (`P125/P126`) and only closes once overwrite is observed without prefill corruption |
| P128 | R | live UVM | none | overwrite-pressure long-soak: pool=1 single-key saturation, push rate 200%, seed=0x23, 1M txn | worst-case overwrite regime on one hotspot; `OVERWRITE_COUNT` dominates `POP_COUNT` and stresses the upper range of the counter |
| P129 | D | live UVM | none | overwrite-pressure directed replay: deterministic single-key throughput shape intended to recreate the historical `P003` failure window under the current checkpoint helper | directed replay locks in the historical hotspot shape and keeps overwrite accounting auditable for future regression reruns |

# Sector-Lock Concurrency, Drop-Counter Regimes, And Metadata-Lineage Soaks (P130-P137)

These cases stress the v26.2.9 sector-lock mechanism, the v26.2.8 64-bit accounting CSRs, and the v26.2.10 metadata-preserving deassembly FIFO under randomized long runs. They directly back BUG-065-R and BUG-066-R closure and feed [FORMAL_PLAN.md](FORMAL_PLAN.md) bounded-liveness data.

| case_id | method | implementation | legacy alias | scenario | primary checks |
|---|---|---|---|---|---|
| P130 | R | live UVM (sv) | none | sector-lock concurrency rate: 32-channel ASIC0-like multi-key random ingress (`pool=64`, lane-local sweep, gap=11 cyc, `EXPECTED_LATENCY=2000`), 200k hits | the sector-lock observer reports `decision_reg=5` for ≥10% of cycles where `pop_engine_state ∈ {LOAD, COUNT, DRAIN}`; max deassembly-FIFO occupancy stays below `DEASM_DEPTH_CONST` and never plateaus the way the BUG-065-R baseline did; catches any regression that re-serializes push behind pop |
| P131 | R | live UVM (sv) | none | sector-lock vs deassembly residency: same shape as `P031` adversarial two-key alternation but with the new sector-lock observer enabled, 200k hits | per-hit deassembly residency histogram has a bounded p99 below the legacy global-lock measurement (the BUG-065-R 2042556 ns plateau); push/pop close at `200k/200k`; `OVERWRITE_COUNT==0` and `CACHE_MISS_COUNT==0` |
| P132 | R | live UVM (sv) | none | per-sector fairness audit: random `pool=16` traffic with deliberate skewed key→sector hashing, 100k hits | observed per-sector lock-bit hit count across the run is within ±20% of the analytical sector activity histogram; no sector becomes permanently hot or permanently cold; catches a `snapshot_sector_mask` path that under- or over-counts a single sector |
| P133 | R | live UVM (sv) | none | counter freeze under sustained long-run traffic: random `pool=4` mixed traffic with `csr_counter_freeze` toggled on/off every 1k CSR-read window for 4M hits | every freeze edge captures `debug_msg2_snap` consistently for all eight 64-bit counters (incl. drop counters); high+low halves stay self-consistent across CSR reads; clearing freeze rejoins the live counters within one CSR-read cycle; catches a partial-counter freeze regression |
| P134 | R | live UVM (sv) | none | drop-counter regime separation: three sub-runs each driving exactly one drop regime — (a) sink fast / source bursty so deassembly-full only; (b) sink fast / `EXPECTED_LATENCY=min` / hot key so pop-cmd-full only; (c) sink very slow so egress-not-ready only | each sub-run's matching drop counter increments while the other two stay at zero; `INERR_COUNT`, `PUSH_COUNT`, `POP_COUNT`, `OVERWRITE_COUNT`, `CACHE_MISS_COUNT` retain their independent contracts; catches any drop counter that double-counts or leaks across regimes |
| P135 | R | live UVM (sv) | none | metadata-lineage long soak: random multi-key traffic with random 64-bit metadata words and random `metadata_valid` per beat, sink-ready=Bernoulli p=0.7 to force long deassembly residency, 1M hits | every drained `aso_hit_type2_metadata` / `aso_hit_type2_metadata_valid` matches the queued ingress pair for the same hit, regardless of how many cycles the hit waited in the deassembly FIFO; zero metadata mismatches across the run; catches BUG-066-R if it regresses |
| P136 | R | live UVM (sv, `RING_BUFFER_N_ENTRY=384` variant) | none | non-power-of-two depth sector geometry under random load: `pool=4` random multi-key, 200k hits, depth=384 (so `ADDR_W=9` and the sector partition is uneven across the address space) | `addr_sector(write_pointer)` never produces an out-of-range index; lock-mask popcount tracks distinct snapshot sectors exactly; final push/pop accounting closes; catches a width assumption that only worked at depth 512 |
| P137 | R | live UVM (sv) | none | decision=5 vs decision=2 throughput delta: same 200k-hit traffic shape replayed twice, once with the live sector-lock and once with a forced "always-locked" override (all push_write_sector_locked=1 unless `pop_engine_state` is IDLE) | published throughput delta and decision_reg histogram between the two runs; sector-lock run has measurable higher `aso_hit_type2_valid` cycles per ingress hit and a non-zero share of `decision_reg=5` events; serves as the throughput proof of the BUG-065-R fix |
