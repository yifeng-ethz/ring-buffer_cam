# trash_bin

This folder keeps deprecated plans, temporary debug dumps, old snapshots, and other unclassified material that should not live at the repository top level.

Current buckets:

- `top_level_20260427/`: root-level logs, stash files, wave screenshots, web debug snapshots, and temporary folders moved during the Phase-5 cleanup.

Do not add active build inputs here. Move material back into a named subsystem folder only after assigning clear ownership and updating the relevant README or script path.
